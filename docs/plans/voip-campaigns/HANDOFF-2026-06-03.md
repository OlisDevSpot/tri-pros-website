# Session handoff — voip-campaigns, on main, working-tree pre-commit (2026-06-03)

> **Why this doc:** continuity bridge across `/compact` after a branch confusion (a fix branch had pushed work onto a different head). We are now genuinely on `main` with voip-in-house fully landed and my Section A + B voip-campaigns work sitting **untracked in the working tree**. This handoff is the source of truth; both prior handoffs (`HANDOFF-2026-06-01.md`, `HANDOFF-2026-06-02.md`) are historical artifacts from before the branch confusion was resolved.
>
> **Predecessor handoffs (read for context only, do NOT rely on for current state):**
> - [`HANDOFF-2026-06-01.md`](./HANDOFF-2026-06-01.md) — Phase 0 dashboard ground-truth corrections (still accurate as a record of those decisions)
> - [`HANDOFF-2026-06-02.md`](./HANDOFF-2026-06-02.md) — provider restructure + initial decoupling plan; SUPERSEDED by this doc

---

## Where main actually is (verified 2026-06-03)

**Branch:** `main`  **HEAD:** `81a03394 refactor(voip): services orchestrate, DAL implements`

### Committed on main — voip-in-house Slug A → C is landed

- **5 schemas** (`voip-dids`, `voip-calls`, `voip-messages`, `voip-link-tokens`, `app-settings`) exported via `db/schema/index.ts`
- **5 entities** (`entities/voip-{dids,calls,messages,link-tokens,app-settings}/`) with `dal/server/{crud,queries,mutations}.ts`, `schemas/`, `types.ts`, `lib/{constants,server-spec,visibility}.ts`, `DOCS.md`
- **Twilio provider** as canonical superset client (`providers/twilio/{client.ts, types.ts, constants/, schemas/, webhooks/, DOCS.md}`)
- **6 voip services** (`services/voip/{compliance, voip-calls, voip-messages, voip-routing, voip-dids, voip-link-tokens}.service.ts`) — all return `DalReturn<>`, take `ScopedContext`, orchestrate entity DAL + Twilio client + sibling services
- **`customers` DNC fields** + `voipCampaignStatus` column + `voipCampaignEnrolledAt` / `voipCampaignGraduatedAt` / `leadIntakeError`
- **`lead_sources.voipConfigJSON`** with `campaigns` + `inHouse` policy sub-objects

### Committed on main — codebase conventions

- ✅ **`client-is-the-superset-entry-point`** (`service-architecture.md` §47) — one singleton, all actions hang off it; no `lib/<resource>.ts` action files; no `webhooks/verify.ts`
- ✅ **`provider-directory-shape`** (§89) — `schemas/` (outbound) + `webhooks/` (inbound) sibling of optional `lib/`; explicit anti-pattern callouts
- ⚠️ **NEW: `background-side-effects-via-qstash-jobs`** (§157) — **replaces `after()`**. Critical side-effects MUST use a `createJob`-declared QStash job, dispatched via `await job.dispatchOrThrow({...})` (critical) or `void job.dispatch({...})` (cosmetic — e.g. notifications, analytics). The `2b536cbc` commit confirms this is actively being rolled out (GCal hooks migrated off `after()`).

### Working tree — uncommitted voip-campaigns work (mine, lint-clean)

Per `git status --short`:

```
M  scripts/setup-gcal-cron.ts                                              (incidental)
M  src/shared/config/roots.ts                                              (incidental)
?? docs/plans/2026-06-01-customer-router-dry-cleanup.md                    (other)
?? docs/plans/construction-data-cover-image-r2-migration.md                (other)
?? docs/plans/voip-campaigns/HANDOFF-2026-06-01.md                         (Section A)
?? docs/plans/voip-campaigns/HANDOFF-2026-06-02.md                         (Section A)
?? docs/plans/voip-campaigns/HANDOFF-2026-06-03.md                         (THIS DOC)
?? docs/plans/voip-campaigns/per-lead-source-content.md                    (Section A)
?? docs/plans/voip-campaigns/phase-1-implementation.md                     (Section A)
?? scripts/push-w2-additive.ts                                             (Section A scratch)
?? src/shared/db/schema/voip-campaigns.ts                                  (Section B)
?? src/shared/db/schema/voip-contact-attributes.ts                         (Section B)
?? src/shared/db/schema/voip-contact-sync.ts                               (Section B)
?? src/shared/services/providers/cloudtalk/                                (Section B — full provider)
?? src/shared/services/voip/campaigns/                                     (Section C scaffolding — only lifecycle-mapper.ts)
```

**My Section A + B output, in detail:**

`src/shared/services/providers/cloudtalk/`:
- `client.ts` — `cloudtalkClient` singleton: `upsertContact`, `findContactByPhone`, `getContact`, `editContact`, `updateContactAttributes`, `addTags`, `removeTags`, `listContacts`, `listContactAttributes`, `listCampaigns`, `setCampaignStatus`, `listCalls`, `getCall`, `recordingPath`, `sendSms`, `bulkContacts`, `verifyWebhookSecret`. Plus `CloudtalkApiError` + `CloudtalkResponseValidationError`.
- `types.ts` — `CloudtalkContact`, `CloudtalkCall`, `CloudtalkContactSummary`, `CtCallListRow` + re-exports of raw CT shapes
- `constants/index.ts` — hosts, rate-limit thresholds, bulk cap, `cloudtalkTagNames`, `cloudtalkDispositions`, `cloudtalkContactAttributeAppKeys`, `cloudtalkPricing`
- `schemas/{primitives, contact, call, sms, campaign, bulk}.ts` — wire-level zod (outbound)
- `webhooks/events.ts` — 5-event discriminated union (inbound)
- `DOCS.md` + `README.md` — superset-client pattern + invariants + quirks

`src/shared/services/voip/campaigns/`:
- `lifecycle-mapper.ts` — pure domain mapper (`mapDispositionToStatus`, `mapTagsToStatus`, `statusToTag`). Lives here, NOT in the provider, per `providers-have-no-domain-types-in-signatures`.

`src/shared/db/schema/`:
- `voip-campaigns.ts` — CT identity bridge (`source_slug`, `ct_campaign_id`, `ct_membership_tag`, `ct_status`, `attempts_per_contact`, `hours_between_attempts`)
- `voip-contact-attributes.ts` — CT attribute_id bridge (`app_key`, `ct_attribute_id`, `ct_title`)
- `voip-contact-sync.ts` — per-customer CT contact identity ledger

**NOT yet added to `db/schema/index.ts`** — three new schemas need to be re-exported via the barrel when we commit Section B.

`docs/plans/voip-campaigns/`:
- `phase-1-implementation.md` — W1 (provider scaffold), W2 (3 new schemas — additive), W3 (webhook handler skeleton — uses `after()`, **needs QStash rewrite**), W4 (enrollment service), W5 (lifecycle mapper), W6 (reconciliation cron — already trimmed to lifecycle-tag drift + stuck-enrollment passes), W7/W8/W9 (seed / UI / holiday cron)
- `per-lead-source-content.md` — 3 custom + built-in attribute tables, Bina + HD templates, V1 verification flagged
- `HANDOFF-2026-06-01.md` — Phase 0 dashboard ground-truth (historical, accurate as snapshot)
- `HANDOFF-2026-06-02.md` — provider restructure plan (superseded by THIS doc)

### Stale tracked docs on main (need re-application of Section A edits)

These are committed on main but predate the 2026-05-31 ground-truth corrections + 2026-06-02 decoupling. My Section A edits to these are NOT in the working tree (I edited them, then the branch confusion reverted them back to their pre-A state):

| Tracked doc | Stale because | Section A correction (re-apply) |
|---|---|---|
| `docs/plans/voip-campaigns/EPIC.md` | Decisions log ends 2026-05-28; no 2026-05-31 ground-truth entry; line 68, 70, 300 reference `providers/cloudtalk/webhooks/lifecycle-mapper.ts` (mapper now lives in `services/voip/campaigns/`) | Add the `2026-05-31 to 2026-06-01` decisions log entry + fix the 3 lifecycle-mapper path references |
| `docs/plans/voip-campaigns/cloudtalk-api-research.md` | "HTTP verbs are inverted" (wrong — resource-suffix); "Generated client" (wrong — hand-typed); "6 documented events" (wrong — 5 WAs); references `CLOUDTALK_PHASE0_TRANSFER_TARGET_E164` | Rewrite per the Section A version (resource-suffix verbs + hand-typed SDK lock + 5 WA architecture + dropped env vars) |
| `docs/plans/voip-campaigns/phase-0-cloudtalk-setup.md` | Task 4 "AI VoiceAgent configuration" not dropped; no V1–V11 verification checklist; no Pattern A DID strategy; no Task 3.5 (3 attributes) / Task 3.6 (9 tags); Task 6 "single webhook URL" (not 5 WAs); Task 8 still lists `CT_FAVORITE_AGENT_ID_*` + `CT_CAMPAIGN_*_ID` | Full restructure per Section A: gate rewrite, Task 3.2 Pattern A, new Tasks 3.5 + 3.6, Task 5 10/3hr cadence, Task 6 5 WAs, Task 8 env var purge, Task 10 V1–V11 |
| `docs/plans/voip/INTEGRATION-SEAM.md` | §1 transfer-target still active; §2 6-event dispatch writes to `voip_calls` / `voip_messages` with `source='cloudtalk'`; §5 `services/compliance.service.ts` (wrong path); §8 still mentions `voip_dnc` table | Apply Step 2 corrections: §2 (5 events, NO shadow writes, dispatches to `lifecycle.service` + `complianceService`), §3 (drop `voip_webhook_errors`), §5 (path fix + DncReason enum), §7 (add gates 5+6), §8 (no shadow rows; CT-as-SoT; lightweight metadata only) |

---

## Open decision (gating Section C — still pending your call)

**Where does the attempt counter live?** CT does NOT fire an exhausted webhook; we count `call.ended` events ourselves and emit `cadence_exhausted` when the count hits `voip_campaigns.attempts_per_contact` (default 10).

**You signaled option (a) before the prior interrupt:**

```ts
// On customers schema:
voipCampaignAttempts: integer('voip_campaign_attempts').notNull().default(0),
```

- 1 column on existing table; atomic `UPDATE` in `lifecycle.recordAttempt`
- Resets to 0 on re-enrollment
- No per-attempt audit trail (acceptable Phase 1; add table later if needed)

**Next session: confirm + add the column to `src/shared/db/schema/customers.ts` (no `db:push:dev`).**

---

## Convention update that affects my W3 skeleton

My `phase-1-implementation.md` W3 webhook-handler skeleton currently uses `after(notifyLastInteractingAgent(...))` for the non-STOP SMS notification side-effect. **This is non-conformant** with the now-committed `background-side-effects-via-qstash-jobs` rule.

**Refactor needed before Section C lands:**

1. Declare an agent-notification QStash job:
   ```ts
   // src/shared/services/providers/upstash/jobs/notify-last-interacting-agent.job.ts
   import { createJob } from '../lib/create-job'
   export const notifyLastInteractingAgentJob = createJob(
     'notify-last-interacting-agent',
     async (payload: { customerPhoneE164: string, body: string }) => { /* ... */ },
   )
   ```
2. Register in `src/app/api/qstash-jobs/route.ts` (jobs array).
3. In `route.ts`, replace `after(...)` with:
   ```ts
   void notifyLastInteractingAgentJob.dispatch({ customerPhoneE164: event.from_e164, body: event.text })
   ```
   `dispatch` (not `dispatchOrThrow`) + `void` — notification is cosmetic; silent loss is acceptable, mutation still returns 200.
4. Patch `phase-1-implementation.md` W3 skeleton to match.

**No other side-effect surfaces in voip-campaigns Phase 1 need QStash yet** — lifecycle transitions are direct DB writes inside the same request (intentional; idempotent). The only true fire-and-forget in W3 is the SMS notification.

---

## Section C — write order (post-decision-confirm)

Same plan as `HANDOFF-2026-06-02.md` Section C, with QStash adjustments:

1. **Confirm attempt counter (a)** → add `voipCampaignAttempts` integer column to `customers.ts`. NO push.
2. **`services/voip/campaigns/lib/resolve-customer.ts`** — `resolveCustomerByPhone(phoneE164)` + `resolveCustomerByCtContactId(ctContactId)` (joins via `voip_contact_sync.cloudtalk_contact_id`).
3. **`services/voip/campaigns/lifecycle.service.ts`** — `applyEngagement`, `recordAttempt`, `applyDisposition`. Returns `DalReturn<>`, takes `ScopedContext`, composes `customersDal` mutations + `cloudtalkClient.{addTags, removeTags}` + `lifecycle-mapper.ts`. **Writes go through `entities/customers/dal/server/mutations.ts`**, not raw `db.update` (per `services orchestrate, DAL implements`).
4. **`services/voip/campaigns/campaign-sync.service.ts`** — `syncFromCloudtalk()` calling `cloudtalkClient.listCampaigns()` + `cloudtalkClient.listContactAttributes()` → upserts `voip_campaigns` + `voip_contact_attributes`. **Open sub-question:** how do we map a CT campaign name back to our `source_slug`? Options: (i) naming convention `tri-pros-<source_slug>` parsed at sync, (ii) admin manually edits the row post-sync. Surface to user during write.
5. **`providers/upstash/jobs/notify-last-interacting-agent.job.ts`** + register in `qstash-jobs/route.ts` (per QStash convention).
6. **`src/app/api/webhooks/cloudtalk/route.ts`** — 5-event switch per W3 skeleton (with QStash dispatch, not `after()`).
7. **Add `db/schema/index.ts` re-exports** for `voip-campaigns`, `voip-contact-attributes`, `voip-contact-sync`.
8. **`pnpm tsc + pnpm lint` checkpoint.**

Entity DAL question: do we need full entity scaffolds (`entities/voip-campaigns/`, `entities/voip-contact-attributes/`, `entities/voip-contact-sync/`) with `crud.ts` + `server-spec.ts` etc., matching the voip-in-house pattern? Yes — voip-in-house's commit `835cc3ab feat(voip-in-house): Slug A — scaffold 5 entities + register in CASL` is the precedent. **Section C should include 3 entity scaffolds** mirroring that pattern. This is more work than HANDOFF-2026-06-02 estimated; flag to user before writing.

---

## DO-NOT-do list (prevent regression)

- ❌ Do NOT write to `voip_calls` / `voip_messages` from the cloudtalk webhook handler (decoupling — CT-as-SoT). Lifecycle state + identity bridge + attempt counter are the only persistence.
- ❌ Do NOT recreate `voip_webhook_errors` table or `voip-webhook-errors.service.ts`. `console.error` is the durable record.
- ❌ Do NOT add `lib/<resource>.ts` action files to the cloudtalk provider. Everything goes on `cloudtalkClient` (`client-is-the-superset-entry-point`).
- ❌ Do NOT recreate `webhooks/verify.ts`. Use `cloudtalkClient.verifyWebhookSecret({ url })`.
- ❌ Do NOT use `after()` from `next/server` for side effects. Use a `createJob`-declared QStash job. **This is the newest convention; don't backslide.**
- ❌ Do NOT raw-`db.update` from a service. Push DB writes into `entities/<x>/dal/server/mutations.ts` (`services orchestrate, DAL implements`).
- ❌ Do NOT auto-push to dev DB. User has pending unrelated cleanup. Add schemas to code; surface diff; let user run `pnpm db:push:dev` (NEVER `pnpm db:push` — prod).
- ❌ Do NOT recreate `CT_FAVORITE_AGENT_ID_*` / `CT_CAMPAIGN_*_ID` env vars (Q3 inversion + DB-table pattern).
- ❌ Do NOT reinstall `@hey-api/openapi-ts`. Probes 1–4 documented; hand-typed wins for Phase 1.
- ❌ Do NOT touch the four stale tracked docs on main without explicit user direction. They need re-application of Section A edits, but the user controls the commit cadence.

---

## Suggested commit order (when user gives the signal)

The working tree mixes Section A docs, Section B code, and Section C scaffolding. A clean commit sequence:

1. **`feat(voip-campaigns): 3 new schemas — voip-campaigns + voip-contact-attributes + voip-contact-sync`** — adds the 3 schema files + re-export from `db/schema/index.ts`. No push yet.
2. **`feat(voip-campaigns): cloudtalk superset provider + 5-event webhook zod`** — full `providers/cloudtalk/` directory.
3. **`feat(voip-campaigns): lifecycle-mapper.ts under services/voip/campaigns`** — single file move out of provider.
4. **`docs(voip-campaigns): phase-1-implementation + per-lead-source-content`** — new working docs (already QStash-aware after the W3 refactor above).
5. **`docs(voip-campaigns): apply 2026-05-31 ground-truth corrections to EPIC + cloudtalk-api-research + phase-0-cloudtalk-setup`** — re-apply Section A edits to the stale tracked docs.
6. **`docs(voip): apply CT-decoupling corrections to INTEGRATION-SEAM`** — re-apply Step 2 edits to the stale tracked seam doc.
7. **Section C commits** — entity scaffolds, services, webhook route.

Whether to land 1–6 first (clean foundation) then Section C, or stack them together, is your call.

---

## Outstanding user-side activities (parallel to code)

- 🟡 A2P 10DLC processing (2–4 weeks) — SMS gated.
- 🟡 Phase 0 dashboard setup in progress (V1–V11 in HANDOFF-2026-06-01).
- 🔴 Dev DB cleanup (user's own work; blocks `pnpm db:push:dev` on Section B + C schemas).

---

## Resume prompt for next session (paste post-`/compact`)

```
Resuming voip-campaigns Phase 1 after /compact. We are on main, voip-in-house is fully landed, my Section A + B voip-campaigns work is in the untracked working tree (lint-clean), Section C orchestration is the next phase.

Read in order before responding:
1. docs/plans/voip-campaigns/HANDOFF-2026-06-03.md            ← THIS DOC — current state + Section C plan + commit order + DO-NOT-do list
2. docs/codebase-conventions/service-architecture.md          → client-is-the-superset-entry-point + provider-directory-shape + background-side-effects-via-qstash-jobs
3. docs/plans/voip/INTEGRATION-SEAM.md                        → STALE on main; Section A corrections pending re-application
4. docs/plans/voip-campaigns/phase-1-implementation.md        → working-tree spec; W3 skeleton needs QStash rewrite before code lands
5. src/shared/services/providers/cloudtalk/DOCS.md            → cloudtalkClient method surface (uncommitted but accurate)
6. src/shared/services/voip/compliance.service.ts             → shipped surface we consume
7. src/shared/services/providers/twilio/DOCS.md               → canonical superset-client reference
8. memory/project-voip-campaigns.md                           → auto-loaded

Constraints (non-negotiable):
- pnpm tsc + pnpm lint for verification; NEVER pnpm build
- pnpm db:push:dev only; NEVER pnpm db:push (prod)
- DO NOT auto-push to dev DB — user has pending cleanup
- Provider has NO lib/<resource>.ts action files (superset-client convention)
- Webhook signature verification = client method, NOT a separate file
- Side effects = QStash jobs (createJob + dispatch/dispatchOrThrow), NEVER after() from next/server
- voip-campaigns does NOT write to voip_calls / voip_messages (CT-as-SoT decoupling)
- Services orchestrate; DAL implements (entities/<x>/dal/server/mutations.ts for writes)
- IANA TZ America/Los_Angeles for time logic
- Coding conventions: one component per file, no barrel files, named exports only

DO-NOT-DO list (see handoff doc — short version):
- DO NOT write to voip_calls/voip_messages from cloudtalk webhooks
- DO NOT recreate voip_webhook_errors table
- DO NOT recreate webhooks/verify.ts (use cloudtalkClient.verifyWebhookSecret)
- DO NOT add lib/<resource>.ts files to the cloudtalk provider
- DO NOT use after() for side effects — declare a QStash job
- DO NOT raw-db.update from a service
- DO NOT touch the four stale tracked docs (EPIC, cloudtalk-api-research, phase-0-cloudtalk-setup, INTEGRATION-SEAM) without explicit user direction

First step: confirm with user the attempt-counter decision. They signaled option (a) before the prior interrupt:
  customers.voipCampaignAttempts: integer().notNull().default(0)
Confirm + add column to src/shared/db/schema/customers.ts. NO push.

Second step: ask user whether to commit-as-you-go (per the suggested order in §"Suggested commit order") or land Section C end-to-end first then squash-commit. The working tree has lint-clean uncommitted Section A + B; the user controls commit cadence.

Third step: Section C in the order listed in HANDOFF-2026-06-03.md → "Section C — write order":
  1. lib/resolve-customer.ts
  2. lifecycle.service.ts (with entity DAL scaffold for customers.voipCampaignAttempts mutations)
  3. campaign-sync.service.ts (surface source_slug-mapping question to user)
  4. notify-last-interacting-agent.job.ts (QStash) + register
  5. /api/webhooks/cloudtalk/route.ts (QStash dispatch, not after())
  6. db/schema/index.ts re-export the 3 new schemas
  7. tsc + lint checkpoint

Open sub-question to flag during Section C: do we need full entity scaffolds for voip-campaigns / voip-contact-attributes / voip-contact-sync (matching voip-in-house Slug A's pattern with crud + server-spec + visibility + DOCS.md)? Leaning YES per precedent — confirm with user before writing 3 entity directories.

User is in CT dashboard configuring Phase 0 in parallel. If they report V1–V11 findings, integrate before continuing code.
```

---

## Footer — files in working tree at handoff time

**Code (Section B, lint-clean):**
- `src/shared/services/providers/cloudtalk/{client.ts, types.ts, constants/index.ts, schemas/*, webhooks/events.ts, DOCS.md, README.md}`
- `src/shared/services/voip/campaigns/lifecycle-mapper.ts`
- `src/shared/db/schema/{voip-campaigns, voip-contact-attributes, voip-contact-sync}.ts`

**Docs (Section A, uncommitted):**
- `docs/plans/voip-campaigns/{phase-1-implementation, per-lead-source-content}.md`
- `docs/plans/voip-campaigns/HANDOFF-{2026-06-01, 2026-06-02, 2026-06-03}.md`

**Stale tracked docs that still need Section A edits re-applied (DO NOT TOUCH WITHOUT USER DIRECTION):**
- `docs/plans/voip-campaigns/EPIC.md` (add 2026-05-31 decisions entry + fix lifecycle-mapper paths)
- `docs/plans/voip-campaigns/cloudtalk-api-research.md` (full Section A rewrite)
- `docs/plans/voip-campaigns/phase-0-cloudtalk-setup.md` (full Section A restructure)
- `docs/plans/voip/INTEGRATION-SEAM.md` (Step 2 decoupling corrections to §1, §2, §3, §5, §7, §8)

**Tracked + modified (incidental — NOT my work):**
- `scripts/setup-gcal-cron.ts`
- `src/shared/config/roots.ts`

**Validation:** `pnpm tsc` clean, `pnpm lint` exit 0 against the working tree as of handoff time.
