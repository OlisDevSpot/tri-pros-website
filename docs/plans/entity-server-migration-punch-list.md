# Entity Server System — Migration Punch List

Produced by the docs context-collapse sweep (2026-05-19). This is the actionable follow-up to ADR-0002's migration order (Proposal → Customer → Meeting → Project) plus the auxiliary entities that don't yet have entity-server-system shape.

**Status snapshot**: Proposals shipped in PR #207. Customers shipped on branch `refactor/customer-entity-server-spec` (TBD-PR). Phase 0 of the customer migration wired `spec.update.jsonbMergeColumns` into `createCrudDal.updateImpl` — proposals also gained correct deep-merge behavior as a side effect; verify no regression in proposal-flow partial-update paths.

The four findings categories below describe what's still pre-migration as of `main` after the customer branch lands.

Each ROW is intended to become a GitHub issue (or PR for the smaller ones). Sized rough P0/P1/P2 by blast radius — not deadline urgency.

---

## Findings — by category

### A. Routers still using `agentProcedure` directly + inline `db` calls

These routers haven't migrated to `createEntityRouter` + the entity toolkit. They inline visibility scoping and bypass the standardized CRUD shape. Migration order is ADR-0002's: Customer → Meeting → Project.

| Router | db.* calls | isOmni copies | Priority | Notes |
|---|---:|---:|---|---|
| `src/trpc/routers/meetings.router.ts` | 7 | 16 | P0 | Largest blast radius; touches participants/scope/customer phone-gating |
| `src/trpc/routers/lead-sources.router.ts` | 27 | 0 | P1 | Standalone — no cross-entity coupling |
| `src/trpc/routers/projects.router/business.router.ts` | 1 | 1 | P1 | Project-creation cross-entity orchestration |
| `src/trpc/routers/projects.router/crud.router.ts` | 0 | 0 | P1 | Delegates to `features/project-management/dal/`; should move into `entities/projects/dal/` + use `createCrudRouter` |
| ~~`src/trpc/routers/customers.router/business.router.ts`~~ | ~~2~~ | ~~5~~ | ✅ | **Shipped on `refactor/customer-entity-server-spec`** — see PR for full migration history |
| `src/trpc/routers/customer-pipelines.router.ts` | 0 | 5 | P2 | Composes customers + meetings; not a standalone entity router |
| `src/trpc/routers/schedule.router/activities.router.ts` | direct | 6 | P2 | Aggregate router (no entity); refactor when meetings migrate |
| `src/trpc/routers/agent-settings.router.ts` | direct | 0 | P3 | User settings (not a business entity) |

**Acceptance criteria per issue**:
- All `db.*` calls moved into the entity's DAL (`src/shared/entities/<x>/dal/server/`)
- Router uses `createEntityRouter(spec, factory)` with the entity toolkit's procedures
- CRUD where applicable goes through `createCrudRouter`
- No inline `isOmni`-or-predicate dance; scope middleware handles it
- DAL functions return `DalReturn<T>` (see category C)
- `pnpm tsc` + `pnpm lint` clean, no behavior regression

### B. DAL functions not returning `DalReturn<T>`

Standardized DAL signature is `(ctx: ScopedContext, input: ...) => Promise<DalReturn<T>>`. Files below predate the standard. Query-helper utilities (`src/shared/dal/server/lib/query/*.ts`, `output.ts`, etc.) are correctly excluded — they're composers, not DAL functions.

**True DAL files not yet on standard signature** (subset of the bare grep result):

| File | Priority |
|---|---|
| ~~`src/shared/entities/customers/dal/server/queries.ts`~~ | ✅ Shipped |
| ~~`src/shared/entities/customers/dal/server/crud.ts`~~ | ✅ Shipped |
| `src/shared/entities/meetings/dal/server/google-calendar.ts` | P1 |
| `src/shared/entities/meetings/dal/server/participants.ts` | P1 |
| `src/shared/entities/proposals/dal/server/crud.ts` | P2 (canonical proposalCrud — was generated by createCrudDal but consumed before the migration; verify) |
| `src/shared/entities/users/dal/server/system.ts` | P2 |
| `src/shared/entities/accounts/dal/server/google-calendar.ts` | P2 |
| `src/shared/entities/activities/dal/server/google-calendar.ts` | P2 |
| `src/shared/entities/push-subscriptions/dal/server/queries.ts` | P3 |
| `src/shared/entities/finance-options/dal/server/queries.ts` | P3 |
| `src/shared/entities/media-files/dal/server/queries.ts` | P2 |
| `src/features/project-management/dal/server/*.ts` (4 files) | P1 (moves into `entities/projects/dal/` with Project migration) |
| `src/features/customer-pipelines/dal/server/*.ts` (4 files) | P1 (stays in features, but signature standardizes) |
| `src/features/agent-dashboard/dal/server/get-action-queue.ts` | P2 |
| `src/features/landing/dal/server/projects.ts` | P3 (public read; ScopedContext = SYSTEM_CONTEXT) |

**Acceptance criteria**:
- Function takes `ScopedContext` first arg
- Returns `Promise<DalReturn<T>>` via `dalDbOperation` or hand-rolled wrapping
- Applies `ctx.scope` to WHERE clauses
- Callers updated to either `dalToTrpc()` (tRPC) or inspect `DalReturn` directly (services/jobs)

### C. Internal services importing `db` directly

Per ADR-0003, internal services never import `db` — they accept `ScopedContext` and forward to DAL. Two violations:

| File | Priority | Notes |
|---|---|---|
| `src/shared/services/notification.service.ts` | P2 | Reads + writes push subscriptions; should use `entities/push-subscriptions/dal/` |
| `src/shared/services/accounting.service.ts` | P2 | QuickBooks write-back path; should use `entities/customers/dal/` for qbCustomerId updates |

**Acceptance criteria**:
- Service accepts `ScopedContext` (or `SYSTEM_CONTEXT`) and forwards to DAL
- No `import { db } from '@/shared/db'`
- DAL functions exist (or are created) for the operations the service was inlining

### D. Background jobs importing `db` directly

QStash job handlers should use DAL, not inline DB calls. One violation found:

| File | Priority | Notes |
|---|---|---|
| `src/shared/services/providers/upstash/jobs/create-qb-records.ts` | P2 | Writes `qbCustomerId` etc. — use customers DAL |

(`schedule.router/sync.router.ts` is a router file, not a job; classified under A.)

**Acceptance criteria**:
- Job handler constructs `SYSTEM_CONTEXT` and calls DAL
- No `import { db } from '@/shared/db'` in `services/providers/upstash/jobs/`

### E. `isOmni` dance still inlined (after middleware exists)

These files have not migrated to using middleware-provided `ctx.scope`. The DAL-internal `helpers.ts:67` count (2) and middleware files (2 each in scope-middleware / shareable-middleware) are correct — they CONSTRUCT scope. The rest are violations.

| File | Count | Priority |
|---|---:|---|
| `src/features/customer-pipelines/dal/server/get-customer-pipeline-items.ts` | 20 | P1 |
| `src/trpc/routers/meetings.router.ts` | 16 | P0 (rolled into A) |
| `src/trpc/routers/schedule.router/activities.router.ts` | 6 | P2 (rolled into A) |
| ~~`src/trpc/routers/customers.router/business.router.ts`~~ | ~~5~~ | ✅ Shipped (1 legitimate isOmni retained in `search` for phone-gating + text WHERE — see commit `6f0711f5`) |
| `src/trpc/routers/customer-pipelines.router.ts` | 5 | P2 (rolled into A) |
| `src/features/customer-pipelines/dal/server/move-customer-pipeline-item.ts` | 4 | P1 |
| `src/features/agent-dashboard/dal/server/get-action-queue.ts` | 4 | P2 |
| `src/trpc/routers/dashboard.router.ts` | 2 | P3 (simple — just forwarding) |
| `src/shared/entities/customers/lib/can-see-phone.ts` | 1 | P3 (client-side render helper; non-blocking) |
| `src/shared/domains/pipelines/lib/get-accessible-pipelines.ts` | 1 | P3 |
| `src/features/agent-dashboard/lib/get-sidebar-nav.ts` | 1 | P3 |

Most of these resolve when the parent router migrates (category A). Standalone DAL files (`get-customer-pipeline-items.ts`, `move-customer-pipeline-item.ts`, `get-action-queue.ts`) need direct attention.

**Total**: 71 occurrences of `isOmni` / `can('manage', 'all')`. After migration, this drops to ~7 (the legitimate spots: middleware, DAL helpers, render gates).

---

## Recommended issue grouping

Filing this as ~5 GitHub issues rather than 30+ atomic ones:

1. ✅ ~~**Migrate Customer entity to entity-server-system**~~ — A: `customers.router/business.router.ts` migration + B: `entities/customers/dal/server/{queries,crud}.ts` standardization. (P1). **SHIPPED on `refactor/customer-entity-server-spec`.**

2. **Migrate Meeting entity to entity-server-system** — A: `meetings.router.ts` → `meetings.router/` directory + entity toolkit + B: `entities/meetings/dal/server/{queries,participants,google-calendar}.ts` standardization. Touches 16+ inline `isOmni`. (P0 — largest)

3. **Migrate Project entity to entity-server-system** — A: `projects.router/{crud,business,media,google-drive,showroom-display}.router.ts` migration + B: move `features/project-management/dal/server/*.ts` into `entities/projects/dal/`. (P1)

4. **Migrate Lead Sources entity to entity-server-system** — A: `lead-sources.router.ts` (27 db calls — largest single-router blast) + entity DAL creation. (P1)

5. **Compliance cleanup: services + jobs + lingering DAL files** — C, D, plus the customer-pipelines/agent-dashboard standalone DAL files from E. (P2 — picked up after A items)

Each issue gets `area:backend`, `type:refactor`, `claude` label per existing workflow (memory/reference-github-workflow.md).

## What NOT to do in these issues

- Don't change behavior, only structure. The phone-gating threshold fix (this PR) is a separate concern.
- Don't try to migrate two entities in one PR — each entity is its own diff for reviewability.
- Don't preemptively refactor list queries into CRUD — list is NOT a CRUD slot per ADR-0002.
- Don't add scope middleware to routers that compose multiple entities (`customer-pipelines.router.ts`, `dashboard.router.ts`, `schedule.router/`) — those receive scope from their consumers, not from a single entity spec.

## Status & references

- ADR-0002: `docs/adr/0002-entity-server-system.md` (architecture)
- ADR-0003: `docs/adr/0003-service-provider-architecture.md` (services layer)
- Canonical example: `src/trpc/routers/proposals.router/index.ts`
- Operational rules: `src/trpc/DOCS.md`
- Per-entity business rules (must remain intact across migration): `src/shared/entities/<x>/DOCS.md`
