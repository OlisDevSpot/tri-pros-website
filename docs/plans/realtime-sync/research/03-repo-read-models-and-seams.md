# 03 — Repo read models, visibility predicates & write seams (Electric SQL / TanStack DB fit)

**Date:** 2026-09-07 · **Repo HEAD:** `8df1c206` (2026-08-25) · **Method:** every claim below is read from code (`file:line`), not docs. Line numbers are as of HEAD.

Scope: the "two agents on the same meeting / proposal / customer" scenario — meeting-flow, proposal-flow, customer profile + pipelines, dashboard. Class legend for §1: **A** single-table row read · **B** multi-table join reproducible client-side from raw rows · **C** server-computed / masked / aggregated / external (not reproducible from raw rows without leaking or re-implementing).

> ⚠️ **Stale refs noticed while verifying (per CLAUDE.md "ping on staleness")**
> 1. `docs/permissions/visibility-rules-catalog.md` Part 3 "Sub-entities" table says `proposal_media_files` has "No spec … `authz.ts:12` … leaks on null scope" and `media-files` "No spec at all. Bridge … never wired". Code now has both specs with a `parent` bridge: `src/shared/entities/proposal-media-files/lib/server-spec.ts:38-48` and `src/shared/entities/media-files/lib/server-spec.ts:37-47`; `authz.ts` no longer exists (`ls src/shared/entities/proposal-media-files/dal/server/` → `crud.ts queries.ts`). The proposal-media router is bridged (`proposals.router/procedures.ts:38-41`); the project-media router still runs unscoped on bare `agentProcedure` (`projects.router/media.router.ts:16-60`, acknowledged in the spec comment `media-files/lib/server-spec.ts:27-31`).
> 2. `docs/plans/realtime-sync/2026-09-07-realtime-sync-prd.md` §3 cites `src/shared/entities/customers/dal/server/phone-gating-sql.ts`; the file is `src/shared/entities/customers/lib/phone-gating-sql.ts`.
> 3. Catalog Part 3 MEETINGS row cites `getParticipants` probe at `participants.router.ts:41` → now `:39`; `getInternalUsers` gate at `reads.router.ts:35` → now `:32`. Cosmetic.

---

## 1. Read-model inventory (every `.query(` the four surfaces call)

Scope resolution shorthand: **`ctx.scope`** = `resolveVisibilityScope(spec)` (`src/trpc/lib/middleware/scope-middleware.ts:22-28` → `resolveEffectiveScope` `src/shared/dal/server/lib/scope.ts:37-48`; omni → `null`). Phone mask = `gatedPhoneSql(canSeeUngatedPhone(ability))` (`src/shared/entities/customers/lib/phone-gating-sql.ts:41-52`): `CASE WHEN EXISTS(SELECT 1 FROM proposals p JOIN meetings m … WHERE m.customer_id = customers.id AND p.status IN ('sent','approved')) THEN phone ELSE NULL END` (`:26-30`); ungated for omni / `read LeadsPool` / `ability === null`.

| # | `router.procedure` (client call site) | DAL function | Tables (FROM + JOINs) | Derived / computed | Visibility applied | JSONB returned | Class |
|---|---|---|---|---|---|---|---|
| 1 | `meetingsRouter.reads.list` (`dashboard/page.tsx:18-19`, `dashboard-meetings-calendar.tsx:43`, `dashboard-snapshot-strip.tsx:19`, `meeting-flow/ui/components/table/index.tsx:42`, `dashboard/meetings/page.tsx:25`, `schedule/page.tsx:17`) | `listMeetings` `src/shared/entities/meetings/dal/server/queries.ts:116-252` | `meetings` LJ `customers` LJ `user`; 2nd query `getAllParticipantsForMeetings` `participants.ts:145-164` (`meeting_participants` ⋈ `user`) | phone CASE (`:166`); `hasSentProposalSql` EXISTS (`:167`); `proposalCount` scalar subquery (`:175`); `hasSentProposal`/`hasApprovedProposal` EXISTS over `proposals` (`:176-177`); `pipeline='projects'` filter ⇒ `projectId IS NOT NULL` (`:135-147`); owner/coOwner folded in JS (`:214-248`) | `ctx.scope` = `meetingVisibility` (`meetings/lib/visibility.ts:9` → `userParticipatesInMeeting` `participants.ts:9-18`) via `meetingProcedure` `meetings.router/procedures.ts:20-23` | `contextJSON`, `flowStateJSON` (full `getTableColumns`) | **C** (phone mask; subqueries otherwise B) |
| 2 | `meetingsRouter.reads.getByIdWithJoins` (`meeting-flow.tsx:64`, `create-new-proposal-view.tsx:40`) | `getByIdWithJoins` `queries.ts:264-306` | `meetings` LJ `customers` LJ `customer_profiles` LJ `user` | phone CASE (`:280`), `hasSentProposal` (`:281`), 3 proposal subqueries (`:284-286`), `profileCols()` flatten (`customers/lib/profile-select.ts:11-14`), raw `phone` column stripped (`:271`) | same as #1 | `contextJSON`, `flowStateJSON`, `customer_profiles.additionalPainPoints` | **C** |
| 3 | `meetingsRouter.reads.getInternalUsers` (`participant-picker-content.tsx:53`) | inline `reads.router.ts:30-46` | `user` WHERE role IN (agent, super-admin) | — | CASL verb `assign Meeting` only (`:32`); no row scope | — | **A** |
| 4 | `meetingsRouter.participants.getParticipants` (`participants-slot.tsx:76`, `participant-picker*.tsx`, `use-participant-mutations.tsx:60`) | `getParticipantsForMeeting` `participants.ts:36-49` | `meeting_participants` ⋈ `user` | — | point probe `isOmni \|\| isParticipant(meetingId, me)` (`participants.router.ts:37-44`; `participants.ts:23-34`) — **not** `ctx.scope` | — | **B** |
| 5 | `meetingsRouter.crud.getById` (server-side only: `customer-pipelines.router.ts:101`, `proposals/dal/server/crud.ts:35`) | `getByIdImpl` `src/shared/dal/server/lib/create-crud-dal.ts:77-93` | `meetings` | — | `ctx.scope` (`:85`) | `contextJSON`, `flowStateJSON` | **A** |
| 6 | `meetingFlowRouter.getPersonaProfile` (`persona-profile-panel.tsx:203`) | `getByIdWithJoins` + `getCachedPainPoints` (Notion, `meeting-flow/lib/get-cached-pain-points.ts:5-12`) + `buildPersonaProfile` (pure TS, `lib/build-persona-profile.ts`) | #2's tables + Notion | fears/benefits/decision-drivers derivation from profile + `contextJSON` + `flowStateJSON` + pain-point DB | `buildUserContext(…, meetingServerSpec)` (`meeting-flow.router.ts:60`) | reads all three | **C** (pure TS — movable client-side if pain points fetched separately) |
| 7 | `proposalsRouter.business.getFullView` (`proposal-flow/dal/client/queries/use-get-proposal.ts:16` → every proposal-flow view; homeowner path with `?token`) | `getFullView` `src/shared/entities/proposals/dal/server/queries.ts:89-156` | `proposals` LJ `meetings` LJ `customers`; + `listProposalIncentives` (`proposal-incentives/dal/server/queries.ts:14-24`, `proposal_incentives`); + `listHomeownerProposalMedia` (`proposal-media-files/dal/server/queries.ts:104-110`, `proposal_media_files` WHERE `visibility='homeowner'`) | `projectFirstContractSentAt` = correlated `MIN(COALESCE(…))` over sibling proposals via `meetings.project_id` (`:113-118`); media URL JIT (`toProposalMediaView` `:33-49`); **customer.phone is RAW here (`:100`) — no gate** | `proposalShareableProcedure` (`proposals.router/procedures.ts:44`): session ⇒ `proposalVisibility` (`proposals/lib/visibility.ts:9`); token ⇒ `scope = eq(proposals.token, token)`, `ability = null` (`shareable-middleware.ts:39-47`) | `projectJSON` (+ frozen `formMetaJSONDeprecated`, `fundingJSONDeprecated` via `getTableColumns`) | **B** (aggregate reproducible from sibling proposal rows; token principal needs its own shape) |
| 8 | `proposalsRouter.business.list` (`dashboard/page.tsx:20-21`, `dashboard-proposal-section.tsx:31`, `dashboard-snapshot-strip.tsx:20`, `proposal-flow/ui/components/table/index.tsx:45`, `dashboard/proposals/page.tsx:25`) | `listProposals` `queries.ts:164-278` | `proposals` LJ `proposal_views` LJ `meetings` LJ `customers` GROUP BY (`:261`) | `viewCount=count(views)`, `lastViewedAt=max` (`:249-250`); `sentRecency=coalesce(sentAt,createdAt)` sort (`:237`); `awaitingSignature`/`sentNoContract` predicates (`:206-224`); pipeline filter on `meetings.projectId/pipeline` (`:188-199`) | `ctx.scope` = `proposalVisibility` via `proposalProcedure` (`procedures.ts:27-30`) | `projectJSON` + blobs | **B** |
| 9 | `proposalsRouter.business.getFinanceOptions` (`use-get-finance-options.ts:6`) | `getFinanceOptions` `finance-options/dal/server/queries.ts:5-12` | `finance_options` | — | none (public `baseProcedure`) | — | **A** |
| 10 | `proposalsRouter.crud.getById` (no client caller in surfaces; router-only) | `getByIdImpl` | `proposals` | — | shareable (spec.shareable ⇒ `shareableProcedure`, `create-crud-router.ts:89`) | `projectJSON` + blobs | **A** |
| 11 | `proposalsRouter.views.getProposalViews` (`proposal-view-badge.tsx:16`) | `getProposalViews` `proposal-views/dal/server/queries.ts:31-53` | `proposal_views` | JS totals/last/email/direct (`:46-52`) | `isInScope(proposalServerSpec)` legacy point probe (`:36`; `scope.ts:90-97`) | — | **A** |
| 12 | `proposalsRouter.contracts.getContractStatus` (`use-contract-status.ts:20`, polled 30 s while `inprogress` `:21-37`) | `getFullView` + `contractService.getContractEnvelopeStatus` (Zoho live) `contracts.router.ts:33-69` | #7 + external | terminal short-circuit from stamps (`:53-60`) | shareable | `projectJSON` | **C** (external API) |
| 13 | `proposalsRouter.contracts.evaluateEnvelopeContext` (`envelope-configuration-section.tsx:58`) | `getFullView` + `evaluateDocuments`/`projectAgreementDocs` (`contracts.router.ts:113-147`) | #7 | registry evaluation on `customer.age`, `kind`, SOW length (pure TS) | shareable | `projectJSON` | **C** (pure TS — movable) |
| 14 | `proposalsRouter.media.list` (`proposal-media-manager.tsx:38`, `use-proposal-media.ts:9`) | `mediaService.list` → `listMediaByOwner` `media-files/dal/server/media-ops.ts:24-37` | `proposal_media_files` | `toProposalMediaView` URL JIT | `proposalMediaProcedure` bridge `proposalId IN (SELECT proposals.id WHERE <proposalVisibility>)` (`procedures.ts:38-41`; `scope.ts:50-57`) + `isVisible` probe + `update Proposal` verb (`media.router.ts:61-68`) | `optimizationVariants` | **A** |
| 15 | `customersRouter.crud.getById` (`customer-search.tsx:26`) | override → `getCustomer` `src/shared/entities/customers/dal/server/queries.ts:67-99` (`customers.router/crud.router.ts:41`) | `customers` LJ `customer_profiles` LJ `customer_lead_attribution`; 2nd query `customer_enrichment` (`:91-95`) | phone CASE + `hasSentProposal` (`:47-54`) | `ctx.scope` = `customerVisibility` (`customers/lib/visibility.ts:8-15`: `read LeadsPool` ⇒ `leadsPoolVisibility` `dal/server/visibility.ts:22-27`, else `userCanSeeCustomer` `:9-19`) via `customerProcedure` (`customers.router/procedures.ts:25-28`) | `additionalPainPoints`, `captureJSON` | **C** |
| 16 | `customersRouter.business.list` (`dashboard/customers/page.tsx:25`, `customers-table.tsx:32`) | inline `customers.router/business.router.ts:49-95` | `customers` LJ `lead_sources` | `derivedPipelineSql()` CASE with `EXISTS projects` / `EXISTS meetings` (`customers/lib/derived-pipeline-sql.ts:26-34`); `derivedPipelineWhere` filter (`:57`); `db.$count` (`:93`) | `ctx.scope` | — | **C** (derivation reads sibling rows the client may not be allowed to sync) |
| 17 | `customersRouter.business.search` (`customer-search.tsx:41`) | inline `business.router.ts:100-130` | `customers` | phone CASE + `hasSentProposal`; omni-only phone `ILIKE` (`:113-118`) | `ctx.scope` | — | **C** |
| 18 | `customerNotesRouter.crud.getById` (no direct client caller; notes arrive via #20) | `getByIdImpl` | `customer_notes` | — | `customerNoteVisibility` (`customer-notes/lib/visibility.ts:16` → `userCanSeeCustomer(userId, customerNotes.customerId)`) | — | **A** |
| 19 | `customerPipelinesRouter.getCustomerPipelineItems` (`customer-pipeline-view.tsx:47`) | `getCustomerPipelineItems` `src/features/customer-pipelines/dal/server/get-customer-pipeline-items.ts:20-36` → 4 branches | fresh (`:148-333`): `customers` ⋈ `meetings` GROUP BY + 3 follow-ups (`proposals`⋈`meetings`⋈`customers` agg `:186-201`; `meetings`⋈`user` DISTINCT ON `:206-222`; `proposals`⋈`meetings` `:236-252`); projects (`:341-511`): `projects`⋈`customers`, `meetings`⋈`user`, `proposals`; rehash/dead (`:95-142`): `customers`⋈`meetings` DISTINCT ON; leads (`:44-63`): `customers` | `bool_or(scheduledFor > now())` / `interval '2 hours'` (`:161-166`); `array_agg(DISTINCT status)` (`:190`); `computeFreshStage` pure TS (`shared/domains/pipelines/lib/compute-fresh-stage.ts:13`); `computePipelineValue`/`computeProjectValue` (`compute-pipeline-value.ts:30,40`); phone CASE; `deriveProjectStatusBucket` | **hand-rolled**: `isOmni ? undefined : userParticipatesInMeeting(...)` per child query (`:173,198,220,249,400`); leads branch **no scope at all** (`:60-62`); projects branch **wider than canonical**: `ownerId = me OR isPublic = true OR participation` (`:367`) vs `projectVisibility` participation-only (`projects/lib/visibility.ts:11-22`) | — | **C** |
| 20 | `customerPipelinesRouter.getCustomerProfile` (`customer-profile-modal.tsx:29`, `create-project-form.tsx:71`, `create-meeting-form.tsx:60`) | `getCustomerProfile` `src/features/customer-pipelines/dal/server/get-customer-profile.ts:35-252` (7 sequential queries) | `customers` LJ `customer_profiles` LJ `customer_lead_attribution` (`:38-52`); `customer_enrichment` (`:63-67`); `meetings` (`:74-87`); `proposals` LJ `proposal_views` GROUP BY (`:89-115`); `customer_notes` LJ `user` (`:176-190`); `proposal_views` (`:192-208`); `projects` (`:211-221`) | phone CASE (`:42`); `hasSentProposal`; JSONB path `projectJSON->'data'->'sow'->0->'trade'->>'label'` + raw `->'sow'` then JS parse (`:99,101,117-153`); `count(views)`; `hasRecording` from `captureJSON.mp3RecordingKey` (`:72`); `deriveProjectStatusBucket` | customer row: `viewer.isSuperAdmin ? undefined : userCanSeeCustomer` (`:51`); **all child rows unscoped** (meetings `:85-87`, proposals `:104-116`, notes `:187-190`, views `:193-208`, projects `:219-221`) — "children ride the parent gate" | `additionalPainPoints`, `captureJSON`, `projectJSON` (sow) | **C** |
| 21 | `customerPipelinesRouter.getRecordingUrl` (`customer-recording-player.tsx:25`) | inline `customer-pipelines.router.ts:71-94` | `customers` LJ `customer_lead_attribution` + R2 presign (`:88-91`) | presigned URL | **none** (no scope, no probe) | `captureJSON` | **C** (external) |
| 22 | `customerPipelinesRouter.getCustomerProjects` (`assign-project-dialog.tsx:35`) | inline `customer-pipelines.router.ts:97-118` | `meetings` (scoped `meetingCrud.getById`), then `projects` by `customerId` (`:105-109`), `proposals` by `meetingId` (`:112-116`) | `deriveProjectStatusBucket(pipelineStage)` (`:111`) | meeting probe via `buildUserContext(meetingServerSpec)` (`:100`); children unscoped | — | **B** |
| 23 | `dashboardRouter.getActionQueue` (`dashboard-action-queue.tsx:30`, `action-center-view.tsx:21`) | `getActionQueue` `src/features/agent-dashboard/dal/server/get-action-queue.ts:118-225` | `proposals` LJ `meetings` LJ `customers` LJ `proposal_views` GROUP BY (`:120-142`); `meetings` LJ `customers` (`:145-157`) | phone CASE (`:124`); JSONB trade path (`:126`); `count/max` views; `classifyProposal` tiering on `Date.now()` (`:61-108`); `ATTENTION_OUTCOMES` filter (`:155`; `constants/enums/meetings.ts:114`) | `isOmni ? undefined : userParticipatesInMeeting` (`:136,154`) | `projectJSON` path | **C** |
| 24 | `projectsRouter.crud.list` (`dashboard/page.tsx:22-23`, `dashboard-project-section.tsx:32`, `dashboard-snapshot-strip.tsx:21`, `dashboard/projects/page.tsx:25`) | `listProjects` `src/shared/entities/projects/dal/server/queries.ts:197-289` | `projects`; 2nd query `x_project_scopes` (`:259-267`) | `statusBucket` ⇒ `coalesce(pipelineStage,'closed') IN stagesForBuckets` (`:217`); `excludePortfolio` ⇒ `hasAssociatedMeeting()` EXISTS `meetings` (`:218`; `projects/lib/visibility.ts:44-51`) | `ctx.scope` = `projectVisibility` (`visibility.ts:31` → `projectParticipationScope` `:11-22`) via `projectProcedure` (`projects.router/procedures.ts:7-8`) | `hoRequirements`, `beforeAfterPairsJSON` | **B** |
| 25 | `projectsRouter.crud.getAll` (no surface caller found; kept for completeness) | `getAllProjects` `:147-175` | `projects` + `x_project_scopes` | — | **none** (bare `agentProcedure`, `crud.router.ts:14-17`) | jsonb ×2 | **B** |
| 26 | `projectsRouter.crud.getForEdit` (`sortable-media-manager.tsx:60`) | `getProjectForEdit` `:114-141` | `projects` + `x_project_scopes` + `media_files` | — | **none** (`crud.router.ts:37-41`) | jsonb + `media_files.tags/optimizationVariants` | **B** |
| 27 | `projectsRouter.showroomDisplay.getAll` (`meeting-flow/ui/components/steps/portfolio-step.tsx:28`) | `getPortfolioProjects` `:15-68` | `projects` WHERE `isPublic` LJ `media_files` (hero) + `x_project_scopes` | JS dedupe | public, single-table predicate `isPublic = true` (`:29`) | jsonb | **B** |
| 28 | `scheduleRouter.activities.list` (`dashboard/schedule/page.tsx:18`) | inline `schedule.router/activities.router.ts:29-90` | `activities` LJ `user` | — | `isOmni ? undefined : eq(activities.ownerId, me)` (`:39`) — **single-table** | `metaJSON` | **B** |
| 29 | `scheduleRouter.activities.getById` | inline `:92-106` | `activities` LJ `user` | — | **none** | `metaJSON` | **B** |

Non-DB reads used by the same surfaces (out of a DB-sync engine's remit): `notionRouter.scopes.getAll/getAllSOW/getSOWContent`, `notionRouter.trades.getAll` (Notion API; `grep "from '@/shared/db'" src/trpc/routers/notion.router/` → none), `projectsRouter.googleDrive.getAccessToken`, `leadSourcesRouter.list`.

**Tally (29 rows):** A = 7 (#3,5,9,10,11,14,18) · B = 10 (#4,7,8,22,24,25,26,27,28,29) · C = 12 (#1,2,6,12,13,15,16,17,19,20,21,23). Weighted by what the *scenario* clients actually render: meeting-flow (#2 C, #6 C, #4 B, #3 A, #27 B), proposal-flow (#7 B, #9 A, #14 A, #11 A, #12 C, #13 C, #2 C), customer profile/pipeline (#20 C, #21 C, #22 B, #15 C, #17 C, #19 C), dashboard (#23 C, #1 C, #8 B, #24 B). The C-class root causes are only four: (i) the phone `CASE` mask, (ii) `EXISTS`-derived flags (`hasSentProposal`, `derivedPipelineSql`), (iii) `now()`-relative bucketing, (iv) external calls (Zoho, R2 presign, Notion). (ii)/(iii) are reproducible client-side only when the client holds *all* the sibling rows the predicate touches — which participation scoping does not guarantee.

Cross-cutting facts about the JSONB payloads a shape would carry: `meetings.flowStateJSON` (7-step flow state, `entities/meetings/schemas/index.ts:68-77`) and `contextJSON` (`:13-18`) are written whole on every meeting-flow keystroke (`meeting-flow.tsx:72-77` → `crud.update`); `proposals.projectJSON` is the SOW blob (NOT NULL) plus two frozen deprecated blobs still selected by `getTableColumns`.

---

## 2. Row-visibility rules a sync engine would have to enforce

The atom: `userParticipatesInMeeting(userId, col)` = `EXISTS (SELECT id FROM meeting_participants WHERE meeting_id = <col> AND user_id = $userId)` — `src/shared/entities/meetings/dal/server/participants.ts:9-18`. Omni (`can('manage','all')`) ⇒ `scope = null` in 4 places: `scope-middleware.ts:26`, `helpers.ts:105-108`, `scope.ts:70-72`, `shareable-middleware.ts:64-65`. "Single-table WHERE" below means: expressible on the target table's own columns with no subquery, i.e. what an Electric shape `where` can take.

| Table | Predicate (verbatim shape) | `file:line` | Single-table WHERE? |
|---|---|---|---|
| `meetings` | `EXISTS(meeting_participants WHERE meeting_id = meetings.id AND user_id = $me)` | `meetings/lib/visibility.ts:9`; `participants.ts:9-18` | **No** (correlated EXISTS). Nearest single-table form: `id IN (<literal list of my meeting ids>)`, re-derived from a `meeting_participants WHERE user_id = $me` shape and re-subscribed whenever participation changes. |
| `customers` (agent) | `EXISTS(meetings ⋈ meeting_participants ON meeting_id WHERE meetings.customer_id = customers.id AND user_id = $me)` | `customers/dal/server/visibility.ts:9-19`; selected in `customers/lib/visibility.ts:8-15` | **No** (two-table EXISTS). |
| `customers` (dispatcher, `read LeadsPool`) | `pipeline = 'active' AND NOT EXISTS(meetings WHERE customer_id = customers.id)` | `customers/dal/server/visibility.ts:22-27`; grant `abilities.ts:223` | **No** (NOT EXISTS). First conjunct is single-table. |
| `customers.phone` (column mask) | `CASE WHEN EXISTS(proposals ⋈ meetings … status IN ('sent','approved')) THEN phone ELSE NULL END`; bypass omni / LeadsPool / `ability=null` | `customers/lib/phone-gating-sql.ts:26-52`; applied at `customers/dal/server/queries.ts:51`, `meetings/dal/server/queries.ts:166,280`, `customers.router/business.router.ts:123`, `get-customer-pipeline-items.ts:49,100,153,353`, `get-customer-profile.ts:42`, `get-action-queue.ts:124` | **No** — a per-row column mask depending on sibling `proposals`+`meetings` state. Electric shapes have column selection but no per-row column masking; a sync engine must either omit `phone` for agents entirely or serve it from a separate "unlocked customers" shape whose membership changes when a proposal is sent. Note the bypass at `proposals/dal/server/queries.ts:100` (raw phone on `getFullView`). |
| `proposals` (agent) | `EXISTS(meeting_participants WHERE meeting_id = proposals.meeting_id AND user_id = $me)` | `proposals/lib/visibility.ts:9` | **No**. |
| `proposals` (homeowner / token) | `proposals.token = $token`, `ability = null` | `shareable-middleware.ts:39-47`; `proposals/lib/server-spec.ts:34` | **Yes** — single-table equality; but the read model (#7) then joins `meetings`/`customers`/`proposal_incentives`/`proposal_media_files(visibility='homeowner')`, each of which needs its own token-derived shape. |
| `projects` (canonical) | `EXISTS(meetings ⋈ meeting_participants WHERE meetings.project_id = projects.id AND user_id = $me)` | `projects/lib/visibility.ts:11-22,31-33` | **No**. (DOCS `#project-visibility-scope` also specifies a negative-outcome exclusion that is *not implemented* — `projects/DOCS.md:107-113`.) |
| `projects` (pipeline board, drift) | `owner_id = $me OR is_public = true OR EXISTS(meetings ⋈ mp … project_id = projects.id)` | `get-customer-pipeline-items.ts:367` | Partially — first two disjuncts are single-table; the third is not. Unresolved fork (catalog Part 5). |
| `projects` (public site) | `is_public = true` | `projects/dal/server/queries.ts:29,74` | **Yes**. |
| `meeting_participants` | No spec / no `visibility` fragment. Read model returns *all* participants of a meeting after a point probe `isParticipant(meetingId, me)`; `manageParticipants` is verb-gated `assign Meeting` | `participants.router.ts:37-47,59`; `participants.ts:23-34` | "My rows" (`user_id = $me`) is single-table **Yes**; "all rows of my meetings" is **No** (`meeting_id IN (<my meetings>)`). Row contains only `meeting_id,user_id,role` — low leak risk if over-served, but `user.name/email/image` joined in `participants.ts:42-44`. |
| `customer_notes` | `EXISTS(meetings ⋈ meeting_participants WHERE meetings.customer_id = customer_notes.customer_id AND user_id = $me)`; write-side author gate `assertNoteAuthorOrAdmin` | `customer-notes/lib/visibility.ts:16`; `customer-notes/dal/server/crud.ts:58-72` | **No**. |
| `customer_profiles` | No own predicate; rides `customers` scope on read (leftJoin `queries.ts:79`), parent probe on write (`customers/dal/server/mutations.ts:37-46`) | — | **No** (inherits customer's EXISTS). PK is `customer_id`. |
| `proposal_media_files` | parent bridge `proposal_id IN (SELECT proposals.id FROM proposals WHERE <proposalVisibility>)`; homeowner subset `visibility = 'homeowner'` | `proposal-media-files/lib/server-spec.ts:41`; `scope.ts:50-57`; `proposal-media-files/dal/server/queries.ts:108` | **No** for agents (bridge); **Yes** for the homeowner subset (`proposal_id = $id AND visibility = 'homeowner'`). |
| `media_files` (project media) | spec bridge `project_id IN (SELECT projects.id WHERE <projectVisibility>)` exists but router runs **unscoped** today | `media-files/lib/server-spec.ts:40`; `projects.router/media.router.ts:16-60` (bare `agentProcedure`) | **No** (when the bridge is switched on). |
| `activities` | `owner_id = $me` (omni: none); `getById`/`complete` unscoped | `schedule.router/activities.router.ts:39,92-106,191-205` | **Yes** — the only in-scope entity whose visibility is a single-table predicate. |
| `proposal_views` | read: `isInScope(proposalServerSpec, id)` point probe; write: unscoped insert | `proposal-views/dal/server/queries.ts:36`; `mutations.ts:19` | **No** (parent-derived); rows carry `ip_address`/`user_agent` (`schema/proposal-views.ts:14-15`) and are already returned raw to agents (`queries.ts:51`). |

Live over-trust the engine must not widen (all verified): `meeting-flow.router.ts:40` `upsertCustomerProfile(SYSTEM_CONTEXT, …)` (verb-checked, row-unscoped); `contracts.router.ts:218` `customerCrud.update(SYSTEM_CONTEXT, …)`; `get-customer-profile.ts:85-221` children unscoped; `get-customer-pipeline-items.ts:60-62` leads branch unscoped; `customer-pipelines.router.ts:76-81` `getRecordingUrl` unscoped; `projects.router/crud.router.ts:14-17,37-41,47-69` unscoped (`scope: null`, documented as #285 tail); `scope.ts:67-70` `isVisible` returns `true` for `SYSTEM_CONTEXT`.

---

## 3. Write-path seams

**Engine.** `createCrudDal` (`src/shared/dal/server/lib/create-crud-dal.ts:51-73`) is the only generic mutation path: `createImpl :97-126`, `updateImpl :130-198`, `deleteImpl :202-246`, `duplicateImpl :250-285` (routes through `createImpl` `:284`). Hook onion: factory `before` → callsite `before` → Zod parse → write → callsite `after` → factory `after` (`:107-124`, `:140-196`, `:230-244`). `ctx.scope` is ANDed into every write's WHERE (`:183`, `:235`). `updatedAt` is bumped by Drizzle `$onUpdate` (`src/shared/db/lib/schema-helpers.ts:13-22`), not by a DB trigger — raw SQL writes that bypass Drizzle do not bump it.

**The single seam for a central "row changed" publish.** Immediately after each write returns, before hooks: `create-crud-dal.ts:118` (`let result = inserted`), `:190` (`let result = updated`, with `previousRow` + `input.data` already in `meta` `:191`), and `:237-239` (after `exec.delete … returning`). Three lines in one file cover every entity that owns a `createCrudDal` instance: meetings (`meetings/dal/server/crud.ts:20`), customers (`customers/dal/server/crud.ts:19`), proposals (`proposals/dal/server/crud.ts:20`), projects (`projects/dal/server/crud.ts:19`), customer-notes (`customer-notes/dal/server/crud.ts:21`), proposal-media (`proposal-media-files/dal/server/crud.ts:12`), media-files (`media-files/dal/server/crud.ts`), lead-sources. Caveat carried from the engine: there is **no post-commit phase** — `withTx` (`helpers.ts:77-84`) threads a tx and any hook/publish fires pre-commit (`helpers.ts:65-75`, `meetings/dal/server/crud.ts:22-33`). A logical-replication consumer is the only writer-agnostic post-commit signal in this codebase.

**Client-origin mutations confirmed to route tRPC → service/DAL → `createCrudDal`:**
- meetings: `crud.*` (`meetings.router/crud.router.ts:13-17`); `business.setOutcomeWithReason` → `meetingCrud.update` + `customerNoteCrud.create` (`business.router.ts:52-73`); `manageParticipants` → `meetingCrud.update` for `ownerId` (`participants.router.ts:107,132,139,200`) **but** participant rows themselves are raw (below); `customerPipelines.assignToProject` → `meetingCrud.update` with `scope: null` (`customer-pipelines.router.ts:130-137`); `moveCustomerToPipeline` → `meetingCrud.update(SYSTEM_CONTEXT)` per row (`move-customer-to-pipeline.ts:34-36`); `moveCustomerPipelineItem` fresh branch → `meetingCrud.update`/`proposalCrud.update` (`move-customer-pipeline-item.ts:123-128,153-160`); `deriveOutcomeOnProposalSent`/`…AdditionalWorkApproved` → `meetingCrud.update` (`meetings/dal/server/mutations.ts:42-45,72-75`).
- customers: `crud.*`; `profile.upsert` → `upsertCustomerProfile` → `upsertOneToOne` (raw `INSERT … ON CONFLICT`, `upsert-one-to-one.ts:52-56`) — **not** through `createCrudDal`; `meetingFlow.updateCustomerProfile` same path with `SYSTEM_CONTEXT` (`meeting-flow.router.ts:40-43`); `createFromIntake` → `customerIntakeService.ingestLead` → `customerCrud.create` + `customerNoteCrud.create` + `meetingCrud.create` (`customer-intake.service.ts:76,115,128,144`) + raw attribution/enrichment upserts (`customers/dal/server/mutations.ts:74-79,88,117`).
- proposals: `crud.*` (duplicate override `duplicate.ts:30-49` → `proposalCrud.duplicate` + raw incentive clone); `delivery.sendProposalEmail` → `proposalCrud.update` (`delivery.router.ts:53-56`); contracts → `contractService.*` → `proposalCrud.update` (`contracts.service.ts:29,78,99,131,158,206`); `applyEnvelopeContext` → `customerCrud.update(SYSTEM_CONTEXT)` + `proposalCrud.update` (`contracts.router.ts:218-221,250-253`); `incentives.replace` → raw tx delete/insert on `proposal_incentives` + `recomputeProposalFinancials` (`proposal-incentives/dal/server/mutations.ts:53-62`); `funding.setCashInDeal` → **raw** `db.update(proposals)` (`proposals/dal/server/mutations.ts:78-80`); `recomputeProposalFinancials` → **raw** `db.update(proposals)` (`:30-40`) — called from the crud hooks (`crud.ts:47,72`) so every proposal create/financial update produces a *second* row change.
- projects: `crud.create/update/delete` → `createProjectWithScopes`/`updateProjectWithScopes`/`projectCrud.delete` with `scope: null` (`projects.router/crud.router.ts:51,61,67`); `business.create` → `projectCrud.create` + `meetingCrud.update` + `setProjectScopes` (`business.router.ts:41-71`); `x_project_scopes` raw (`projects/dal/server/mutations.ts:14,18`).
- customer-notes: `crud.*` only (`customer-notes.router/index.ts:10-14`).

**Mutations that bypass `createCrudDal` (raw `db.*`/`tx.*`) on the in-scope tables:**

| Table | Site | Origin |
|---|---|---|
| `meeting_participants` | **all** writes: `participants.ts:174` (insert), `:178` (delete), `:191` (update) | `participants.router.ts:89,130-131,138,159-162,175,180,207,210`; `meetingCrud.create.after` `crud.ts:64` |
| `meetings` | `meetings/dal/server/google-calendar.ts:105,111,117,126` (`gcal_*`, `scheduled_for`) | GCal inbound sync `scheduling.service.ts:105,120,122,128,233,246,253,291,355` ← `api/google-calendar/webhook/route.ts:26`, `sync-calendars`/`initial-calendar-sync` jobs |
| `customers` | `voip/compliance.service.ts:68,83` (DNC), `customers/dal/server/measurement.ts:37` (`meta_schedule_sent_at`), `customers/dal/server/queries.ts:221-233` (`findOrCreateCustomerFromHomeowner` insert) | JustCall webhook `api/webhooks/justcall/route.ts:58,89`; `meta-capi-event` job; funnel ingestion |
| `customer_profiles` | `upsert-one-to-one.ts:52-56` | `meeting-flow.router.ts:40`, `customers.router/profile.router.ts:26` |
| `customer_lead_attribution` / `customer_enrichment` | `customers/dal/server/mutations.ts:74-79,88,117` | intake service, funnels router |
| `proposals` | `proposals/dal/server/mutations.ts:30` (rollup), `:78` (cash-in-deal), `providers/ai/client.ts:105-113` (`projectJSON` merge), `customers/dal/server/crud.ts:112` (cascade delete) | crud hooks; funding router; `generate-ai-summary` job; customer delete |
| `proposal_incentives` | `proposal-incentives/dal/server/mutations.ts:54,59,94` | incentives router; duplicate |
| `proposal_views` | `proposal-views/dal/server/mutations.ts:19` | `views.recordView` (`systemProcedure`, token-gated) |
| `projects` | `move-customer-pipeline-item.ts:78-81` (`pipeline_stage` drag) | pipeline board (agent) |
| `customer_notes` | `landing.router/index.tsx:137-141` | public website forms |
| `activities` | `schedule.router/activities.router.ts:121,175,195,227`; `activities/dal/server/google-calendar.ts:39-72` | agent UI; GCal sync |
| `media_files` / `proposal_media_files` | `media-files/dal/server/mutations.ts:26,52,54`, `optimization.ts:17,36,40,44`, `media-ops.ts:55` | project media UI; `optimize-media` job |

**Non-client writers (never pass through a browser's optimistic path):** QStash jobs registered in `src/app/api/qstash-jobs/route.ts:32-55` — `sync-zoho-sign-status` (→ `contractService.applyContractEvent(SYSTEM_CONTEXT)` `:41` → `proposalCrud.update` + meeting outcome derive, `contracts.service.ts:206-211`), `sync-meeting-to-gcal`/`delete-meeting-event`/`sync-calendars`/`initial-calendar-sync` (raw `meetings.gcal_*`), `propagate-customer-change`, `create-qb-records`/`sync-qb-invoice`/`sync-qb-payment` (→ `accountingService` → `customerCrud.update`/`projectCrud.update`/`proposalCrud.update(SYSTEM_CONTEXT)` `accounting.service.ts:77,117,167,218,238`), `generate-ai-summary` (raw `proposals.projectJSON`), `optimize-media` (raw status), `meta-capi-event` (raw `customers.meta_schedule_sent_at`), `graduate-from-campaign`/`enroll-*`/`bulk-*` (voip tables). Webhooks: Zoho (`api/webhooks/zoho-sign/route.ts:49-53` → job), Google Calendar (`api/google-calendar/webhook/route.ts:26` → inbound sync), JustCall (`api/webhooks/justcall/route.ts:58-63,89-95` → DNC + unenroll), Bina (`api/webhooks/bina/route.ts:35` → `ingestLead(SYSTEM_CONTEXT)`), QuickBooks. `SYSTEM_CONTEXT` (`dal/server/types.ts:44-48`) sites: 39 files (`grep -rln SYSTEM_CONTEXT src`).

---

## 4. Existing realtime footprint (Ably)

| Piece | `file:line` |
|---|---|
| Server REST client | `src/shared/services/providers/upstash/realtime.ts:27` — `lazyProxy(() => new Ably.Rest({ key: getAblyConfig().apiKey }))`; design comment `:7-25` ("client IGNORES the event payload") |
| Server env | `ABLY_API_KEY` optional in `src/shared/services/providers/upstash/lib/config.ts:62-64`, spread into `server-env.ts:100`; `requiredKeys: ['ABLY_API_KEY']` `:75` |
| Browser client | `src/shared/services/providers/upstash/realtime-client.ts:7-12` — `process.env.NEXT_PUBLIC_ABLY_API_KEY!` (not in any Zod env schema; only occurrence in repo), `new Ably.Realtime({ key, autoConnect: typeof window !== 'undefined' })` |
| Provider mount | `src/shared/components/providers/realtime-provider.tsx:6-12` (`AblyProvider`) → `providers/index.tsx:14` (second-outermost, inside `TRPCReactProvider`) → `src/app/(frontend)/layout.tsx:144` — i.e. **every page in `(frontend)` incl. the marketing site opens the Ably socket** |
| Channel subscribe | `src/features/meeting-flow/ui/views/meeting-flow.tsx:48-50` `ChannelProvider channelName={\`meeting:${meetingId}\`}`; `:60` `useMeetingSync(meetingId)`; `:191` `<SyncStatusIndicator status=…/>` |
| Sync hook | `src/features/meeting-flow/hooks/use-meeting-sync.ts:11-26` — `useChannel(\`meeting:${meetingId}\`, invalidate)` (`:23`), `useConnectionStateListener` (`:15`), calls `invalidateMeeting()` with **no args** (`:20`) |
| Status UI | `src/features/meeting-flow/ui/components/sync-status-indicator.tsx:9-27` |
| Publish #1 | `src/shared/entities/meetings/dal/server/crud.ts:144-146` — `update.after` → `ably.channels.get(\`meeting:${row.id}\`).publish('meeting.updated', { fields: Object.keys(data) })`, **unguarded await** (a throw fails the mutation; caveat comment `:22-33,111-113`). Fires for every `meetingCrud.update` regardless of origin (agent, SYSTEM_CONTEXT job, webhook via `applyContractEvent` → `deriveOutcome…`). No publish on create/delete. |
| Publish #2 | `src/trpc/routers/meeting-flow.router.ts:49-52` — after `upsertCustomerProfile`, `.catch(console.warn)`. The sibling path `customers.router/profile.router.ts:17-27` has **no** publish. |
| Commented-out / planned | `src/shared/entities/customers/dal/server/crud.ts:62-67` (`customer:<id>` publish sketch); `src/shared/services/scheduling.service.ts:406-409` (`@migration(ably-realtime-kernel)`) |
| Fan-out | `src/shared/dal/client/hooks/use-invalidation.ts:49-54` `invalidateMeeting` → `meetingsRouter.pathFilter()`, `customerPipelinesRouter.getCustomerPipelineItems`, `getCustomerProfile` (all when no id), `dashboardRouter.pathFilter()`; `invalidateProposal :56-64` adds `leadSourcesRouter`; `invalidateCustomer :43-47`; `invalidateProject :66-76`. Every mutation hook calls one of these on success (`use-update-proposal.ts:11-13`, `meeting-flow.tsx:73,80`, `use-participant-mutations.tsx:56`, `customer-profile-modal.tsx:32-34`). |
| Query-client defaults | `src/trpc/query-client.ts:12` `staleTime: 30_000`; `refetchOnWindowFocus` left at TanStack default (true); RSC prefetch via `src/trpc/server.ts:8-13` + `src/trpc/lib/prefetch.ts:18-25`; client link is `httpBatchLink` (`providers/trpc-provider.tsx:21`), not streaming |
| Polling in lieu of realtime | `use-contract-status.ts:21-37` (30 s while `inprogress`); `customer-pipeline-view.tsx:55-60` refetch on every move; `edit-project-view.tsx:38` media-optimization poll |
| Coverage gaps | no channel for `customers`, `proposals`, `customer_notes`, `meeting_participants`, `projects`, `activities`; second browser on the customer-profile modal or proposal-flow gets nothing; job/webhook writes to non-meeting tables are invisible |

---

## 5. Schema facts for logical replication / shapes

Source: `src/shared/db/schema/*`. `id` helper = `uuid().primaryKey().defaultRandom()` (`db/lib/schema-helpers.ts:3`); `unsafeId` = `serial('id')` (`:4`); `updatedAt` = `timestamptz defaultNow() $onUpdate(() => new Date().toISOString())` (`:13-22`) — **Drizzle-side, no DB trigger** (`grep -rn trigger src/shared/db/schema` → only column names). All `user`-FK columns are `text` (better-auth ids), not uuid. DB client = `node-postgres` `Pool` (`src/shared/db/index.ts:24-28`); `@neondatabase/serverless` is in `package.json:53` but unused (`grep -rln` → none).

| Table | PK | pg enums (`meta.ts`) | text-enum cols | JSONB | Array | Generated | Other |
|---|---|---|---|---|---|---|---|
| `meetings` (`meetings.ts:21-39`) | uuid | `meeting_type`, `meeting_outcome`, `meeting_pipeline` (`meta.ts:38,40,43`) | — | `context_json`, `flow_state_json` (`:30-31`) | — | — | `owner_id` text FK cascade; `customer_id`/`project_id` set-null; `gcal_*` ×3 |
| `customers` (`customers.ts:11-63`) | uuid | — | `lead_type`, `pipeline` (`:32,36`) | — | — | — | `phone` text (masked in reads); `dnc_*`; `latitude/longitude` double |
| `customer_profiles` (`customer-profiles.ts:37-69`) | **`customer_id` uuid (PK-as-FK)** | — | ~14 `text(…,{enum})` | `additional_pain_points` (`:45`) | — | — | lazy-upserted (~12% of customers) |
| `proposals` (`proposals.ts:18-99`) | uuid | — | `status`, `kind`, `price_display_mode` | `form_meta_JSON` (deprecated), `project_JSON` NOT NULL, `funding_JSON` (deprecated) (`:44-47`) | `envelope_document_ids text[]` (`:63`) | — | 6× `bigint` cents; partial unique idx `(meeting_id) WHERE kind='initial-sale' AND status='approved'` (`:96-98`); `token` text (share principal) |
| `projects` (`projects.ts:13-44`) | uuid | — | — | `ho_requirements`, `before_after_pairs_json` (`:24,36`) | — | — | `accessor` unique; `is_public`; `pipeline_stage` text; `owner_id` text FK |
| `meeting_participants` (`meeting-participants.ts:12-33`) | uuid | `meeting_participant_role` (`:20`) | — | — | — | — | `unique(meeting_id,user_id)`; partial unique `meeting_one_owner_idx`/`meeting_one_co_owner_idx` (`:27-32`) |
| `customer_notes` (`customer-notes.ts:8-15`) | uuid | — | — | — | — | — | `author_id` text FK set-null |
| `activities` (`activities.ts:11-28`) | uuid | `activity_type`, `activity_entity_type` (`:13,16`) | — | `meta_json` (`:25`) | — | — | polymorphic `entity_id` uuid (no FK); `gcal_*` |
| `media_files` (`media-files.ts:11-22`) | **serial int** | `media_phase` (`:17`) | `provider` | `tags`, `optimization_variants` (`:15`; `lib/media-columns.ts:36`) | — | — | `path_key` unique; `url` NOT NULL |
| `proposal_media_files` (`proposal-media-files.ts:14-21`) | **serial int** | — | `visibility` ('internal'\|'homeowner'), `provider` | `optimization_variants` | — | — | `path_key` unique; `thumbnail_path_key` |
| (adjacent) `proposal_views` (`proposal-views.ts:8-18`) | uuid | `view_source` | — | — | — | — | **no `updated_at`**; `ip_address`, `user_agent` |
| (adjacent) `customer_lead_attribution` | `customer_id` PK-as-FK | — | `kind` | `capture_json` (`:37`) | — | — | write-once |
| (adjacent) `customer_enrichment` | uuid | — | — | — | — | — | `unique(customer_id, step_id)` |

Totals: **50 `pgTable`** (`grep -h "pgTable(" *.ts | wc -l`; `auth.ts` holds 4), **18 `pgEnum`** (`meta.ts:25-50`), **0** views / materialized views, **0** generated columns, **0** DB triggers. Every in-scope table except `proposal_views` carries `created_at`/`updated_at`.

---

## 6. Deployment / runtime facts

- `next.config.ts:5-81`: no `output`, no `experimental` block; `images.unoptimized`, `allowedDevOrigins`, `serverExternalPackages: ['pdfkit']` (`:41`), `outputFileTracingIncludes` for `/api/qstash-jobs`, `/api/proposals/[proposalId]/pdf`, `/funnels/[trade]/opengraph-image` (`:42-62`), `/sw.js` headers (`:69-80`). No `vercel.json` in repo.
- Runtime exports under `src/app/**`: `export const runtime = 'nodejs'` only at `(frontend)/funnels/[trade]/opengraph-image.tsx:8`; **no `'edge'` anywhere**; single `maxDuration = 60` at `api/qstash-jobs/route.ts:27`; `dynamic = 'force-dynamic'` on all dashboard pages (`dashboard/page.tsx:9`, `meetings/page.tsx:12`, `proposals/page.tsx:12`, `customers/page.tsx:12`, `pipeline/[pipeline]/page.tsx:4`, `proposals/[proposalId]/page.tsx:4`, …); `revalidate` only on marketing/sitemap routes.
- tRPC handler: `src/app/api/trpc/[trpc]/route.ts:5-28` — `fetchRequestHandler` from `@trpc/server/adapters/fetch`, `GET`+`POST`, default Node runtime, per-request session via `auth.api.getSession` (`src/trpc/lib/create-http-context.ts:12-26`, `cache()`-deduped). Client link `httpBatchLink` (`trpc-provider.tsx:21`) — no streaming link, no `after()`/`waitUntil` anywhere (`grep` → only comments in `justcall/route.ts:23,70`).
- Fluid compute: not expressed in code (Vercel project setting) — **UNVERIFIED** here. Long-lived connections today: Ably WebSocket from browser (`realtime-client.ts:9-12`); server side only short REST publishes.
- Deps: `next 15.5.9`, `@trpc/* ^11.4.1`, `@tanstack/react-query ^5.80.7`, `drizzle-orm ^0.45.1`, `pg ^8.16.3`, `ably ^2.21.0`, `better-auth ^1.6.9`, `@upstash/qstash ^2.9.0` (`package.json:53-133`).

---

## 7. Scale hints (from code)

- Pagination: `limit` default **20**, hard max **500** (`src/shared/dal/server/lib/query/schemas.ts:13`); `paginate()` runs page + count queries.
- Dashboard caps (`src/features/agent-dashboard/constants/dashboard-queries.ts:27`): meetings 8, proposals 20, proposalsPerSection 5, projects 15, projectsPerSection 5, actionQueue 8 (client-side cap only); month calendar window `limit: 200` (`:41`). `dashboard/page.tsx:18-23` prefetches 6 list queries per load (2 meetings, 2 proposals, 2 projects); `getActionQueue` is fetched client-side, **unpaginated** — all `status='sent'` proposals + all `ATTENTION_OUTCOMES` meetings in scope (`get-action-queue.ts:120-157`).
- `getCustomerPipelineItems` (`get-customer-pipeline-items.ts`): **unpaginated whole-pipeline scan**; fresh branch = 1 GROUP BY over `customers⋈meetings` + 3 follow-up queries keyed by the full customer-id list (`inArray`, `:199,219,250`) + JS maps; projects branch = 3 queries + JS; leads branch = every customer with no meeting (`:44-63`, no scope, no `limit`); rehash/dead = `DISTINCT ON`. Visibility is re-applied per child query with the EXISTS atom (`:173,198,220,249,400`) — for a non-omni agent each of those is a correlated subquery over `meeting_participants`. The reported drift is confirmed: `:367` (`ownerId = me OR isPublic = true OR EXISTS…`) is wider than `projectVisibility` (`projects/lib/visibility.ts:11-22`); the leads branch is wider than `leadsPoolVisibility` (`customers/dal/server/visibility.ts:22-27`, which also requires `pipeline='active'`).
- `getCustomerProfile`: 7 sequential round-trips per modal open (`get-customer-profile.ts:38,63,74,89,176,193,211`), with two `IN (…literal list…)` clauses built from prior results (`:107-113,201-206`).
- Meeting list is the busiest read model: 6 call sites (§1 #1) each with its own filter/sort key; every Ably ping invalidates all of them plus pipeline/profile/dashboard (`use-invalidation.ts:49-54`).
- Realtime volume drivers: `meeting-flow.tsx:72-77` writes the whole `flowStateJSON` on each flow change → one `meetingCrud.update` → one `meeting.updated` publish per keystroke-level save; `recomputeProposalFinancials` doubles every proposal financial write (`proposals/dal/server/crud.ts:47,72`).

---

## Verdict inputs

- **Read-model classes (29 inventoried):** A ≈ 24% (7) · B ≈ 34% (10) · C ≈ 41% (12). Among the ~20 procedures the two-agent scenario actually renders, C is the majority; the C-ness comes from four reusable causes (phone `CASE` mask; `EXISTS`-derived flags/pipeline; `now()`-relative bucketing; Zoho/R2/Notion calls), not from bespoke per-procedure logic.
- **Row visibility as a single-table WHERE (per entity):** `meetings` **no** · `customers` **no** (both agent and leads-pool forms) · `customers.phone` mask **no** · `proposals` agent **no** / token **yes** · `projects` **no** (canonical; `is_public=true` public subset **yes**) · `meeting_participants` "mine" **yes** / "all of my meetings" **no** · `customer_notes` **no** · `customer_profiles` **no** (inherits) · `proposal_media_files` agent **no** / homeowner subset **yes** · `media_files` **no** · `activities` **yes** (`owner_id = me`). Every "no" is the same correlated `EXISTS(meeting_participants …)` atom (`participants.ts:9-18`) viewed through a different FK; the only single-table approximation is `id IN (<materialised list of my meeting ids>)`, re-derived from a `meeting_participants WHERE user_id = me` shape and re-subscribed on participation change.
- **Central publish seam:** `src/shared/dal/server/lib/create-crud-dal.ts` — post-write points `:118` (create), `:190` (update, `previousRow`/`input` in hand at `:191`), `:237-239` (delete); one file, covers every `createCrudDal` entity. It is pre-commit when a tx is threaded (`helpers.ts:65-84`) and it **misses** the raw writers in §3 (`meeting_participants` entirely, GCal-sync `meetings.gcal_*`/`scheduled_for`, DNC/measurement on `customers`, `customer_profiles` upserts, `proposals` rollup/cash-in-deal/AI-summary, `proposal_incentives`, `proposal_views`, `projects.pipeline_stage` drag, `activities`, media status) — the case for a WAL-level signal over an application-level one.
