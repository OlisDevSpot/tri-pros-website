// Canonical entity-name constant for the PushSubscription entity. Source of truth
// for `EntityName` and `AppSubject` (see domains/permissions/abilities.ts).
// Infrastructure entity — no EntityServerSpec.
export const PUSH_SUBSCRIPTION = 'PushSubscription' as const
