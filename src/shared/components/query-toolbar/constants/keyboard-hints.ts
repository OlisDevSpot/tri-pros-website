/**
 * Muted micro-copy shown in the chip rail when no filters are active and no
 * search term is typed — teaches the keyboard shortcuts without resorting to
 * a tooltip or help icon.
 */
export const KEYBOARD_HINT_TEXT = 'Press / to search · F to filter · ← → to paginate'

/** Key that focuses the search input when no input is focused. */
export const SEARCH_SHORTCUT_KEY = '/'

/** Key that opens the filter trigger when no input is focused. */
export const FILTER_SHORTCUT_KEY = 'f'
