# Convention Auditor — Ledger

Pointers + verification dates only. Docs remain the single source of truth.
Entries older than the doc's last git edit must be re-verified before citing.

## derived-values (last verified 2026-07-23)
- Rule: JIT default; cache column needs all four legs (chokepoint, one pure fn, rebuild script, calc_version); snapshot named for event, never recomputed — source: derived-values.md#persist-vs-derive-decision,#cache-column-discipline,#snapshot-discipline (ratified 2026-07-17, supersedes blanket never-persist)
- Exemplar: `recomputeProposalFinancials` (`entities/proposals/dal/server/mutations.ts:47-66`, SQL at :52-59) + `computeFinalTcp` (`lib/financials/compute-price-side.ts:33`) + `scripts/recompute-final-tcp.ts` (dry-run, routes through chokepoint, :28 "keep in sync" duplicated SQL) + `proposals.calc_version` (`db/schema/proposals.ts:50`, default 1)
- Known drift: ⚠️ `entities/proposals/schemas/index.ts:81-84` comment still phrases the OLD blanket "never persist derived values" doctrine ("Persisted derived values invite drift…always compute on demand") — superseded 2026-07-17; final_tcp_cents IS a sanctioned cache column. Reported 2026-07-23.
- Rulings: recompute wired at server-spec create.after (:64) + update.after (:91); `ai/client.ts:106-114` raw db.update is the ONE registered escape hatch (DEFERRED — AI-summary paused, do not touch).

## proposals — financials façade + lock ladder (last verified 2026-07-23; supersedes 2026-07-09 "#final-tcp-derived never-persist" entry)
- Rules: price-side-vs-cost-side (façade `lib/financials/index.ts` = ONLY money-math import surface; PricingBreakdownModel carries no cost fields); three-stage lifecycle (draft JIT / rollup cache / frozen snapshot); lock ladder 4 tiers via `getProposalLockState` only — sources: proposals/DOCS.md#price-side-vs-cost-side,#final-tcp-derived,#proposal-lock-ladder (all updated ≤2026-07-18)
- Exemplars: `buildPricingBreakdown` (`lib/financials/compute-breakdown.ts:53`) consumed by exactly 3 renderers: `features/proposal-flow/ui/components/pricing-breakdown.tsx`, `shared/lib/pdf/proposal-doc-definition.ts`, `app/api/proposals/[proposalId]/summary/route.ts`; `frozenProposalLockedFields` (`lib/proposal-lock.ts:59-66`: label, formMetaJSON, projectJSON, fundingJSON, financeOptionId, meetingId)
- Rulings: `pricingMode` lives in `formMetaSectionSchema` (`schemas/index.ts:99`); `miscPrice` optional + `startingTcp` required in `fundingDataSchema` (:89-90); NO `_v` on any proposal blob schema yet (mandatory-schema-version triggers on next touch); superRefine (:142-156) requires sectionPrice>0 in breakdown mode only; client sync effect `funding-fields.tsx:77-87` keeps startingTcp = Σ sectionPrice + miscPrice in breakdown mode ONLY (DOCS.md:161 "client-side only today" — matches code).

## jsonb-columns (last verified 2026-07-23)
- Rules used: #mandatory-schema-version (add `_v` next touch), #evolution-playbook (expand-contract), #zod-parse-at-write-boundary, #never-shallow-merge-nested (tombstone) — source: jsonb-columns.md (last edited 2026-07-15)
- Known drift: ⚠️ jsonb-columns.md:41,134,238 reference `docs/domain/ubiquitous-language.md` — `docs/domain/` does not exist; actual path is `docs/ubiquitous-language.md`. Reported 2026-07-23.
- Ruling: sanctioned precedent for deleting a NON-money JSONB key = Zod-strip, no migration (`showPricingBreakdown`, façade spec :208). Money-bearing keys need a data migration first.

## jsonb-deprecation-ledger + migration vocabulary (last verified 2026-07-23)
- Rules: unchecked ledger row = scheduled scaffolding, never early-delete; drop protocol (4 steps) binds every frozen-column DROP; tightening tally per UL #migration--contract-change-vocabulary (docs/ubiquitous-language.md:226-239); seam-tightening register = the program's tally
- Key W3-bridge rows touching proposal financials: getFullView incentive hydration, recompute jsonb residues (startingTcp base + section-incentives term, with the W3 sow_item_id ordering constraint), blank-writers (edit/create views + create-proposal-popover), blob-wide freeze gap, scrubBlobIncentives tripwire (`295c20b1`)
- Ruling: façade spec (2026-07-14, marked implemented 2026-07-18) assigns server-side enforcement of `startingTcp = Σ sectionPrice` to Wave 3 explicitly (spec :70); Wave 3 pre-registered scope = proposal_sow_items + proposal_cost_lines + section incentives → rows + starting_tcp_cents + signing_request_id rename + batched W1/W2 drops.

## forms (last verified 2026-07-23)
- Rule: ALL forms = RHF + zodResolver + nuqs; no raw useState — source: memory feedback-form-conventions (NO codebase-conventions/forms.md exists; candidate for promotion via README decision tree)
- Exemplar: proposal form (`proposalFormSchema` + `proposalFormShape` split for ZodEffects derivation, `schemas/index.ts:122-142`); derived form values via useWatch effect (`funding-fields.tsx:42-87`)
- Known drift: memory doc says schema file is `schemas.ts` in entity dir; actual convention is `schemas/index.ts` (schemas/ dir, sibling-of-lib). Minor.

## service-architecture (last verified 2026-07-09)
- Rule: four-tier split; pure-local utility never lives in `services/providers/` — source: docs/codebase-conventions/service-architecture.md#the-deciding-question + Anti-patterns
- Exemplar: `src/shared/services/pdf.service.ts` (internal service, ScopedContext, DAL-only reads) (verified 2026-07-09)
- Known drift: ⚠️ service-architecture.md:105-121 (`provider-directory-shape`) cites `sanitize-filename.ts` as the example provider `lib/` file — will go stale when the proposal-pdf-export spec relocates it to `src/shared/lib/sanitize-filename.ts` (reported 2026-07-09; propose doc edit in same PR)
- Rulings: `sanitizeFilename` has TWO importers (`zoho-sync.service.ts:6`, `providers/zoho-sign/lib/documents/assemble-envelope.ts:6`), not one as the 2026-07-09 spec claims.

## notification.service raw-db read debt (last verified 2026-07-27)
- Drift: `src/shared/services/notification.service.ts` imports `db` and does `db.select` reads directly (violates dal-conventions "only DAL imports db"). Pre-existing, carries an explicit `@migration(meetings-entity-router)` tombstone (:13-19). The 2026-07-27 push ADDED a new `notifyNewLead` method (:65) with two more `db.select` reads (customers + user lookup) — extends the acknowledged debt rather than routing through customerCrud/DAL. Reads-only; NO db.insert/update/transaction in any service (hard mutation rule intact). Severity: MINOR drift, not a prod blocker. Fix: fold the customer read into a customers DAL query + resolve recipients via a users DAL query when the meetings-entity-router migration lands (same tombstone).

## dal-conventions / backend layering (last verified 2026-07-09)
- Rule: only DAL imports `db`; services/routes go through DAL with ScopedContext/SYSTEM_CONTEXT — source: docs/codebase-conventions/dal-conventions.md + memory coding-conventions Rule 19
- Exemplar: `src/app/api/proposals/[proposalId]/summary/route.ts:1-34` (SYSTEM_CONTEXT + token gate); `src/shared/services/pdf.service.ts:27-34` (generateSowPdf shape) (verified 2026-07-09)
- Known drift: ⚠️ memory `coding-conventions.md` Rule 15 key-files table says `shared/dal/server/lib/types.ts`; actual file is `src/shared/dal/server/types.ts` (SYSTEM_CONTEXT at line 41). `lib/` holds helpers.ts/create-crud-dal.ts only. Reported 2026-07-09.

## frontend-stack (last verified 2026-07-09)
- Rule: one component per file, named exports, no file-level constants/helpers, lucide-only icons, shadcn over native HTML, motion/react — source: docs/codebase-conventions/frontend-stack.md
- Exemplar: `src/features/proposal-flow/ui/components/navbar/navbar.tsx` (hasMounted+CASL pattern at 24-32) (verified 2026-07-09)
- Known drift: `src/features/proposal-flow/ui/components/proposal/view-mode-toggle.tsx` exports 3 components + a local hook in one file (pre-existing violation of one-component-per-file); `ViewModeToggleMobile` uses raw `<button>` (Rule 17 drift). Both are removal/refactor targets of the 2026-07-09 pdf-export spec.

## urls-and-origins (last verified 2026-07-09)
- Rule: app-page paths via ROOTS.*; ESLint `project/no-raw-nav-paths` guard — source: docs/codebase-conventions/urls-and-origins.md
- Ruling: the lint regex (`eslint.config.js:6-7`) deliberately covers app-page prefixes only, NOT `/api/*`. ROOTS has no API-route builders; precedent builds API URLs inline (`providers/upstash/lib/create-job.ts:19` — `publicUrl('/api/qstash-jobs?...')`). Client hrefs to first-party API routes: template literal acceptable; put the builder in a feature `lib/` helper when 2+ consumers.

## webhook-routes (last verified 2026-07-09)
- Rule: async webhooks → `/api/webhooks/<provider>`; sync request-response → `/api/<domain>/<purpose>` — source: docs/codebase-conventions/webhook-routes.md (async-vs-sync split)
- Exemplar: `/api/proposals/[proposalId]/summary` (first-party sync-read under domain namespace) (verified 2026-07-09)

## proposals entity (last verified 2026-07-09)
- Rules used: `#shareable-via-token` (token IS authorization), `#final-tcp-derived` (never persist; computeFinalTcp), token-not-a-secret anti-pattern — source: src/shared/entities/proposals/DOCS.md
- Exemplar: summary route token gate `summary/route.ts:15-34`; `computeFinalTcp` use at `summary/route.ts:121` (verified 2026-07-09)

## proposal-flow feature (last verified 2026-07-09)
- Rules used: `#view-mode-defaults-to-customer-casl-gates-agent` (CASL gate inside useViewMode — never read `?view` directly); cost lines agent-only (Anti-patterns) — source: src/features/proposal-flow/DOCS.md
- Exemplar: `hooks/use-view-mode.ts`; gate pattern in navbar.tsx:30 (verified 2026-07-09)

## pdf pipeline (last verified 2026-07-09)
- Rule: pdf generation is a shared lib (`src/shared/lib/pdf/`), NOT a provider — source: service-architecture.md "Current classification" line 323
- Exemplars: `sow-doc-definition.ts` (styles: Roboto→Helvetica, 10pt, pageMargins [56,56,56,56]); `render-pdf.ts:30` renderPdf(def): Promise<Buffer>; pdfkit tracing at `next.config.ts:41-45` (serverExternalPackages + per-route outputFileTracingIncludes glob) (verified 2026-07-09)

## enum-standardization — Closed Vocabulary Standard (last verified 2026-07-24; SUPERSEDES 2026-07-09 pgEnum entry)
- Rule: const array stays source of truth, but storage default is now `text('col', { enum: constArray })` — NO pgEnum without a documented DB-side consumer (zero exist as of 2026-07-14). Legacy pgEnums convert opportunistically when a migration touches their table — source: enum-standardization.md#text-with-enum,#pgenum-only-with-db-side-consumer,#legacy-pgenum-conversion (ratified 2026-07-14, commit 8c89e751); spec Addendum C
- Exemplars: `src/shared/db/schema/customer-profiles.ts:40` (`text('trigger_event', { enum: triggerEvents })`); `proposal-incentives.ts:19` (`text('type', { enum: incentiveTypes })`). W2 converted 4 pgEnums→text; 19 remain for opportunistic conversion.
- Known drift: ⚠️ TWO `incentiveTypes` const arrays exist — `constants/enums/proposals.ts:22` (5 values: discount, tax-credit, cash-back, exclusive-offer, other; imported by `funding-fields.tsx` UI, which filters to 2) vs `entities/proposals/schemas/index.ts:61` (2 values: discount, exclusive-offer; drives the DB column + Zod). Violates #const-array-source-of-truth "declared once". Reported 2026-07-24 — consolidate during W3 incentive work.
- Note: database-schema.md#pgenum-placement/#pgenum-uses-const-array still read as if pgEnum were routine — they now govern the exception case only (doc not updated for Addendum C; enum-standardization.md carries the new default). APPEND-to-end rule only matters for surviving pgEnums.

## db-migrations workflow (last verified 2026-07-09)
- Rule: schema sync via `pnpm db:push:dev` (drizzle-kit push), NOT hand-authored SQL migration files; NEVER `pnpm db:push` (prod) — source: database-schema.md:82; CLAUDE.md
- Known drift: none. Note: appended enum value emits `ALTER TYPE ... ADD VALUE` under push. Touchpoint framing "write a Drizzle migration" conflicts w/ repo convention (push, not generate).

## casl-abilities (last verified 2026-07-09)
- Rule: one `case '<role>'` block per role; field-level grants via 3rd-arg array; new subject → ENTITY_NAMES/AppSubject — source: abilities.ts:11-16 HOW-TO-EXTEND
- Exemplar: `src/shared/domains/permissions/abilities.ts:89` (agent), :93 (field-level Customer update)
- Known drift: meetings/DOCS.md#ownership-model's `can('delete','Meeting',{ownerId})` marked PLANNED, not implemented — documented, not hard drift.

## visibility-scope-middleware (last verified 2026-07-09)
- Rule: `EntityServerSpec.visibility: (userId:string)=>SQL`; `scope = isOmni ? null : spec.visibility(userId)` — source: trpc/DOCS.md#scope-middleware-is-the-core-superpower; types.ts:69
- Exemplar: `scope-middleware.ts:19-21`; `helpers.ts:71` (buildUserContext)
- Known drift: none. RISK: signature is a hard contract in scope-middleware.ts:20 + helpers.ts:71 + every `entities/*/lib/visibility.ts`. `leads` bucket (active customer, no meeting) is invisible to participation-scoped agents BY CONSTRUCTION (`userCanSeeCustomer` requires a meeting participation).

## phone-gating (last verified 2026-07-09)
- Rule: agent phone only via `gatedPhoneSql(...)` at DAL; unlock threshold proposal status IN('sent','approved') — source: customers/DOCS.md#phone-visibility-threshold; phone-numbers.md
- Exemplar: `phone-gating-sql.ts:33`
- Known drift: param named `isSuperAdmin` but ALL ~10 consumers pass `isOmni` (ability.can('manage','all')/scope===null/ability==null). Semantic = "ungated", not "is-super-admin". Doc says "super-admins always see it" while code ungates all omni+SYSTEM. Rename→capability on dispatcher refactor.

## meetings-ownership (last verified 2026-07-09)
- Rule: create.before forces `ownerId=session.user.id` for authed (anti-spoof); SYSTEM_CONTEXT passes ownerId through — source: meetings/DOCS.md#ownership-model,#meeting-owner-is-creator; server-spec.ts:49-54
- Exemplar: `meetings/lib/server-spec.ts:49`; `getSystemOwnerId` at `entities/users/dal/server/system.ts`
- UNBUILT: `sales_agent` role, `isDispatched` derivation (`lib/is-dispatched.ts`), "info@ owns = unassigned dispatch inbox" all PLANNED (meetings/DOCS.md#participant-roles-are-meeting-contextual Status:PLANNED, #dispatched-derived). Current roles owner/co_owner/helper. Dispatcher meeting "lands in inbox" has NO consumer yet.
- Ruling: extending create.before IS the sanctioned extension point (hooks contract types.ts:93).

## pipeline-access (last verified 2026-07-09)
- Rule: `getAccessiblePipelines(ability)` binary — omni→all, else hardcoded `['projects','fresh']` — source: get-accessible-pipelines.ts:7,14. Sidebar `get-sidebar-nav.ts` is CASL-driven (`ability.can(...)`).
- Known drift: none, but binary design; a role needing a DIFFERENT set (`leads`) forces role/ability-driven refactor, not a boolean fork.

## sub-entity-standard — 1:many child tables (last verified 2026-07-24)
- Rules: Addendum B / ADR-0005 (amended 2026-07-14): 1:many child = own PK + FK cascade + `position` when ordered; NO CASL subject/router unless own verbs; reads via batch-fetch `inArray`; 1:1 child = PK-as-FK + `upsertOneToOne` + own CASL subject — sources: dal-conventions.md#one-to-one-child-tables, jsonb-columns.md#sub-entity-decision-tree, spec §10 (Addendum B)
- Exemplars: 1:many — `db/schema/proposal-incentives.ts` (id/FK-cascade/position/text-enum/CHECKs/amount_cents bigint); 1:1 — `db/schema/customer-profiles.ts` + `upsertCustomerProfile`. Multi-parent inArray batch fetch: `push-subscriptions/dal/server/queries.ts:83`; single-parent ordered: `listProposalIncentives` (`proposals/dal/server/queries.ts:159`, position-ordered, sowItemId IS NULL).
- Rulings: child mutation pattern = scoped parent probe (`ctx.scope` in WHERE) + `isProposalFrozen` gate + delete-then-insert in one `db.transaction` + `recomputeProposalFinancials` + return fresh rows (`replaceProposalIncentives`, mutations.ts:78-113). CRUD `duplicate` never copies children — override precedent `duplicateProposalWithIncentives` (`dal/server/duplicate.ts`, own file ABOVE crud+mutations to avoid import cycle). Child tRPC procedures live as a named sub-router inside the OWNING entity's router with explicit `ctx.ability.cannot('update','Proposal')` check on the PARENT subject (`proposals.router/index.ts:64-79`).
- Money: integer cents at DAL boundary, dollars in domain shape — mapper pair `lib/incentive-rows.ts` (Math.round(x*100) / /100; pg text-mode timestamptz→ISO normalization at :20). No codebase-conventions money doc — canonical is proposals/DOCS.md#final-tcp-derived changelog row v1 ("integer cents, no floats stored") + schema comments.

## scripts — env/target conventions (last verified 2026-07-24)
- Rule: scripts import the app's `@/shared/db` singleton; prod ONLY via `DRIZZLE_TARGET=prod` prefix (package.json prod scripts carry it explicitly); `describeTargetDb` operator banner; `--dry-run` flag; parity = read-back deep-equal diff (order-independent — pg jsonb doesn't preserve key order) + per-row Zod gate, non-zero diff ⇒ exit 1 — source: environment.md#environment-axes
- Exemplars: `scripts/backfill-wave2-children.ts` (THE W3 backfill precedent: parity harness, TS computeFinalTcp pins SQL recompute, cutover-window-only warning header); `scripts/recompute-final-tcp.ts` (rebuild-script leg of cache-column discipline)
- Ruling/nuance: memory feedback-scripts-load-env ("import './lib/load-env'") applies only to scripts that DON'T transitively import `server-env` — `@/shared/db` → `server-env.ts:20-21` already does .env.local-first dotenv loading; backfill-wave2-children.ts and recompute-final-tcp.ts import no load-env and are correct.

## proposal lock ladder — code shape (last verified 2026-07-24)
- Rule: all lock checks derive from `getProposalLockState`/`isProposalFrozen` (`lib/proposal-lock.ts:35,49`) — never ad-hoc field checks. Parent update gate is FIELD-SCOPED via `touchesFrozenLockedFields` (`frozenProposalLockedFields` :59-66) in update.before (`server-spec.ts:74-86`); child-row mutations carry their OWN gate (probe + isProposalFrozen, mutations.ts:97).
- W3 exposure: `frozenProposalLockedFields` lists parent columns only — new child tables (sow_items, cost_lines) and incentive rows are frozen only if every child mutation repeats the gate. Ledger's "blob-wide freeze gap" row tracks this; extending the gate to child tables is explicit W3 scope.
- Rename fact: `proposals.contractEnvelopeId` maps to DB column `signing_request_id` (`db/schema/proposals.ts:35`) — the W3 prod-push rename target.

## query-toolkit (last verified 2026-07-24)
- Rules: always-use-usepaginatedquery; paginated-input-via-composer; filter-types-via-registry; reserved-url-key-suffixes (p,q,sort,dir,ps); param-prefix-when-multiple-tables — source: query-toolkit.md
- Exemplars: hook `src/shared/dal/client/hooks/use-paginated-query.ts` (input assembly :126-179; parser map built inline :105-117); keys `client/lib/url-state.ts:15` (makeQueryParsers returns KEY NAMES, not parsers); registry `client/lib/filter-parser-registry.ts` (FIVE types incl. number-range; imports from 'nuqs' → client-only today); page-size options `client/lib/constants.ts:6`
- Known drift: ⚠️ query-toolkit.md:10-14,:85,:93 cite `src/shared/dal/client/lib/query/` — directory does not exist (actual: `client/hooks/` + `client/lib/`). ⚠️ :73-82 filter table lists 4 types (registry has 5: number-range missing) and says date-range URL = `from_to` underscore format (actual parseAsJson → JSON-encoded). Reported 2026-07-24; fix rides the prefetch-infra docs amendment.
- Rulings: DEFAULT_PAGE_SIZE=20 + DEFAULT_DEBOUNCE_MS=250 are file-level consts in the hook (:58-59); customers table uses paramPrefix 'pc' + CUSTOMER_FILTER_CONFIG (`entities/customers/constants/customer-filter-config.ts`, isomorphic-safe deps) but leaves SHOW_COLUMNS as a file-level const in `customers-table.tsx:27` (pre-existing no-component-file-constants violation).

## prefetch-hydration infra — LANDED (last verified 2026-07-27, pre-prod gate on 79-commit push)
- Prior 2026-07-24 entry called this all greenfield/absent; it has now shipped. New canonical facts:
  - `src/trpc/server.ts` stayed `.ts` (NOT renamed to .tsx) — the ErrorBoundary compose lives in separate `src/trpc/components/hydrate-client.tsx` + `hydration-error-boundary.tsx` ('use client'), so the rename-ripple I predicted did NOT happen. Server.ts now uses `createRSCTRPCContext` (correct headers-based ctx), not the old broken getSession-without-headers. Drift from 2026-07-24 RESOLVED.
  - `prefetch()` is fire-and-forget void-only (`src/trpc/lib/prefetch.ts:37`), infinite-query discriminator + keyShape assert. Awaited/blocking variant removed by design (frontend-stack.md#server-prefetch-two-tiers:30).
  - `react-error-boundary@^6.1.2` IS in package.json deps + really used (`hydration-error-boundary.tsx:4`). Not phantom.
  - Shared-table-config contract (query-toolkit.md#shared-table-config, frontend-stack.md#server-prefetch-two-tiers): every paginated table exports ONE `PaginatedQueryConfig` from `constants/`, consumed by both page `loadPaginatedQueryInput(searchParams, CONFIG)` and view `usePaginatedQuery(...,CONFIG)`. Exemplars (all verified 2026-07-27): `entities/customers/constants/customers-table-query-config.ts`, `features/proposal-flow/constants/proposals-table-query-config.ts`. Runtime-options split exemplar = `features/campaigns-admin/constants/campaign-leads-table-query-config.ts` (`options: [] as const` for runtime-populated filters; ids/types static).
  - Isomorphic query builder relocated to `src/shared/dal/lib/query/` (derive-paginated-query-state.ts, url-state.ts, filter-parser-registry.ts) — the neutral `dal/lib/` home I flagged as "unwritten best practice" on 2026-07-24 now EXISTS. Candidate promotion realized.
- Lint rule `project/no-inline-table-config` flipped warn→error (`eslint.config.js`, commit 70074c5b). Aliased to a custom rule id via `builtinRules.get('no-restricted-syntax')` because flat-config last-writer-wins would otherwise clobber `project/no-raw-nav-paths`. `pnpm lint` = clean (exit 0) on this HEAD → CI-safe. `pnpm tsc` = clean (exit 0).
- Pre-existing drift RESOLVED this push: `customers-table.tsx` SHOW_COLUMNS file-level const (flagged 2026-07-24) is GONE.

## trpc server-side proxy / prefetch infra (last verified 2026-07-24; superseded by LANDED entry above)
- Rules: server.ts is 'server-only' + React cache() dedup; never import into 'use client' (use useTRPC from @/trpc/helpers) — sources: trpc-procedures.md#server-only-tRPC-proxy, trpc/DOCS.md#dont-import-server-only-trpc-into-client
- Verified facts: server.ts:13 calls `auth.api.getSession()` WITHOUT headers (always-null session in RSC) + stray `setCookie` not in HTTPTRPCContext (types.ts:57-60); correct impl = `createHTTPTRPCContext` (`trpc/lib/create-http-context.ts:12-26`, cached, headers()-based, re-exported from init.ts). query-client.ts:13-18 ALREADY has superjson serialize + pending-query dehydration (Tier-1 void-prefetch precondition). Only importers of @/trpc/server: 2 type-only imports in resend email templates (rename to .tsx is safe — alias resolves extensionless).
- No HydrationBoundary/useSuspenseQuery/react-error-boundary usage anywhere yet (all greenfield); react-error-boundary NOT in package.json. nuqs 2.8.6; createLoader precedent `src/features/proposal-flow/lib/search-params.ts`.
- Rename ripple: "server.ts" cited at trpc/DOCS.md:12,:274,:279,:317 and trpc-procedures.md:83,:86 — must update to server.tsx in the same PR.
- Ruling: ErrorBoundary+QueryErrorResetBoundary reset wiring (render-prop closures) cannot be composed inline in the server-only file — needs a small 'use client' wrapper component in its own file; server.tsx composes it around HydrationBoundary. LoadingState/ErrorState (`shared/components/states/{loading,error}-state.tsx`) both REQUIRE `title: string`; imported by direct file path (no barrel).

## frontend-stack (re-verified 2026-07-24)
- Same rules as 2026-07-09 entry. Note: named-exports-only is absolute as written, but ALL Next.js pages are `export default async function` (framework requirement, repo-wide precedent — e.g. dashboard/customers/page.tsx:7); the anti-pattern only bans renamed default imports elsewhere.
- #use-client-pushed-to-leaves (:9 "Views that fetch via tRPC are client components") and #views-own-data-fetching both encode client-only fetching — the prefetch-infra work amends these by design.

## analytics feature — convention audit (verified 2026-07-30)
- HARD RULE confirmed: `only-dal-imports-db` (dal-conventions.md#only-dal-imports-db) is elevated to a hard rule by ADR-0002:157 ("DAL is the ONLY path to the database. No file outside `shared/dal/` and `shared/entities/*/dal/` may import `db`… hard rule, not a guideline"). The `domains/` layer is NOT on the allowlist.
- Drift (SEV-HIGH, reported 2026-07-30): `src/shared/domains/analytics/sources/local/{leads,appointments,signed}-per-adKey.ts` each `import { db }` + run raw `db.select().groupBy()` in the domain layer, with a false-justification comment "Raw db aggregation (domain, not service)." No such sanctioned pattern exists. The ONLY other `domains/*` raw-db importer, `permissions/lib/validate-share-token.ts`, is explicitly listed at ADR-0002:162 as a "Direct DB access violation (must migrate to DAL)" — precedent is an acknowledged debt, not a license. Analytics is NET-NEW db-in-domains and lacks even the `@migration(...)` tombstone ADR-0002:177 requires. `auth/server.ts` importing db is NOT a violation (framework adapter handing the ORM handle to better-auth's drizzleAdapter, not a query).
- Spec-vs-convention conflict: design spec §5.5 (2026-07-28-analytics-feature-marketing-sales-design.md) prescribes `sources/local/*.ts # SQL aggregations` in the domain layer — the spec embeds the violation. A feature spec cannot waive a hard ADR rule; fix requires code relocation + spec correction (or an ADR amendment carving a documented exception).
- Duplication: the three local sources share one skeleton (brandedMetaPaidScope + utmContent IS NOT NULL + customers.createdAt range + groupBy utmContent). No query-toolkit aggregation helper exists (query-toolkit = paginated LIST only). Right generalization = one parameterized business-DAL helper (customers or a customer-lead-attribution DAL) returning DalReturn. `brandedMetaPaidScope` was correctly extracted; the query wasn't.
- Service placement RULING: flat `src/shared/services/meta-insights-sync.service.ts` is CORRECT per the four-tier table (Sync service lives at `services/<x>-sync.service.ts`, NOT under providers/). Mirrors flat `meta-sync.service.ts`. Human's suspicion (should be under providers/meta) is unfounded. Nuance: it exposes ONE op (byAd) — `sync-service-when-2-plus-ops` (service-architecture.md:234) says sub-2-op should call the client directly; minor over-abstraction, not a blocker. It correctly does NOT import db (services-never-import-db, :242 — satisfied).
- createdAt cohorting RULING: `createdAt = timestamp('created_at',{mode:'string',withTimezone:true})` (db/lib/schema-helpers.ts:10). `.toISOString()` (UTC-Z) string comparison via gte/lte is CORRECT + repo-consistent (mode:'string' everywhere; matches memory feedback-no-raw-sql-for-event-timestamps). Human's suspicion unfounded.
- Typing: insights Zod IS wired (client.ts:9 import + :127 `metaAdInsightsResponseSchema.parse(await res.json())`) — NOT dead code. `SourceRow=Record<string,unknown>` (types.ts:21) is a resolver-internal loose alias coexisting with the properly-generic builder-facing `Source<Row extends Record<PropertyKey,any>,K>`; cosmetic dual-abstraction, not a bug.
- Unbuilt (unverifiable/greenfield as of 2026-07-30): analytics.router.ts, analytics-snapshots.ts, features/analytics/, snapshot QStash job. `superAdminProcedure` exists (init.ts:83, = agentProcedure + omni gate).

## customer notes + activity-center (last verified 2026-08-05)
- Shape: `customer_notes` = 1:many child of customers (`db/schema/customer-notes.ts`: own `id` PK + `customer_id` FK ON DELETE cascade + `content` + `author_id` text FK→user.id ON DELETE set null + createdAt/updatedAt). No `position` (ordered by createdAt). No CASL subject of its own → follows sub-entity-standard 1:many rules (ledger sub-entity-standard entry).
- Write path today: ONLY `addCustomerNote` (`entities/customers/dal/server/mutations.ts:~34`, plain `db.insert`, wrapped in `dalDbOperation`, returns `DalReturn<CustomerNote>`). Called by intake service, Bina ingest, and `business.addNote` procedure. authorId optional (null = system/webhook).
- Procedure home: `business.router.ts` `addNote` (`createCustomerBusinessRouter`, mounted at `customersRouter.business` in `customers.router/index.ts:~85`). New updateNote/deleteNote belong HERE (entity-owns-its-mutations) + new DAL fns in the SAME mutations.ts.
- ⚠️ DRIFT / gap (reported 2026-08-05): `addCustomerNote` does NOT take ScopedContext and does NOT probe the parent customer against `ctx.scope` — the `addNote` procedure passes only `{customerId, content, authorId}`. This diverges from the child-mutation precedent `replaceProposalIncentives` (`proposals/dal/server/mutations.ts:78`) which does a scoped parent probe (`and(eq(id), ctx.scope ?? undefined)`) before mutating. New updateNote/deleteNote MUST take `ctx` + probe parent customer scope; do NOT copy addNote's unscoped shape.
- No documented business rule for note edit/delete authorship (customers/DOCS.md has ZERO notes/timeline/activity rules — grep confirmed). Author-only-edit vs any-agent-with-customer-access is an UNDECIDED business rule — user must rule, then codify in customers/DOCS.md (Rule 29). `author_id` is nullable + `ON DELETE set null`, so author-name resolution must handle null.
- Author display: `getCustomerProfile` (`features/customer-pipelines/dal/server/get-customer-profile.ts`, note rows via `db.select().from(customerNotes)` = SELECT *, no join) currently returns raw notes (authorId text only). To show name/avatar → add `leftJoin(user, eq(user.id, customerNotes.authorId))` with explicit column select; widen `CustomerProfileData.notes` type from source (drizzle user cols), don't hand-mirror. NOTE this file is a feature-DAL Rule-19 known-migrating db importer (listed in coding-conventions Rule 19 violations) — join is acceptable in-place but perpetuates feature-DAL; canonical would be a customers entity DAL query.
- Frontend exemplars (all `entities/customers/components/timeline/`): `customer-timeline.tsx`, `quick-note-input.tsx`, `timeline-event-item.tsx`; pure builder `lib/build-timeline-events.ts` (already carries `metadata.authorId`, `metadata.meetingId`); config `constants/timeline-event-config.ts` (has `TimelineEventCategory = note|meeting|proposal` + stage-safe color rule — reuse for filter chips, don't reinvent).
- ⚠️ DRIFT: `quick-note-input.tsx` uses raw `useState` for the textarea (violates form-conventions RHF+Zod) AND `invalidateCustomer()` (router-level pathFilter, broad) with NO optimistic insert. Redesign should upgrade, not copy: composer → RHF+zodResolver (single-field still counts); invalidation → per-procedure `getCustomerProfile.queryFilter({customerId})` (participant-invalidation rule); insert → optimistic 3-callback pattern.

## forms — single-field composer ruling (2026-08-05)
- Ruling: form-conventions ("ALL forms = RHF+zodResolver, never raw useState", memory feedback-form-conventions) is written as absolute and explicitly covers "single-step, or any many-fields-in-a-related-structure". A single-textarea note composer is a judgment edge — a 1-field textarea with only a trim/min/max check is the weakest case, but the rule as written admits no size exception, and the existing `quick-note-input` raw-useState impl is a KNOWN divergence, not a sanctioned precedent. Bind it to RHF+Zod (content: z.string().min(1).max(2000)) to match the procedure's input schema and get consistent disabled/error handling. Still NO codebase-conventions/forms.md — memory pointer only; promotion candidate stands.

## proposal-media public cutover — Sub-plan 2 blast radius (last verified 2026-08-06)
- Context: audited PROPOSED design (proposal media private/presigned → public tpr-media, render like portfolio). Epic: `docs/superpowers/specs/2026-08-06-media-foundation-epic-design.md` (Global Constraints :15-25 verified).
- `resolveProposalMediaUrl` importers = EXACTLY 3 (verified grep): `trpc/routers/projects.router/media.router.ts:9,152`; `entities/proposal-media-files/dal/server/queries.ts:6,29`; the file itself `lib/resolve-media-url.ts`. Deleting the file + reshaping view atomically = correct tighten (no dual-shape). All 3 rewritten by design → no orphans.
- `ProposalMediaView` consumers (verified): gallery `features/proposal-flow/ui/components/proposal/proposal-media-gallery.tsx` (`.url` at :30,:39,:53,:57 — img+video+pdf); manager `.../form/proposal-media-manager.tsx:28` (`r.url` via toItems); getFullView `entities/proposals/dal/server/queries.ts:156`; router `proposals.router/media.router.ts:60`. All drop `await` when mapper goes sync.
- OptimizedImage.file contract (`shared/components/optimized-image.tsx:9-18`) = {url, pathKey, bucket, optimizationStatus, optimizationVariants?, blurDataUrl?}. Reshaped view must expose all. Manager builds file from its `viewById` map (:40) mirroring project-media-manager `fileById.get(id)!` (:499) — shared `MediaItem` (`components/media/types.ts:9-11`) does NOT need pathKey/bucket. getOptimizedSrc/Set already derive from row bucket domain (`get-optimized-urls.ts:52,82`); require non-null `url` field → picker must synthesize url.
- xs variant: add to `VARIANT_DEFS` + `SIZE_LIMITS` (`media-files/lib/process-image-variants.ts:28-39`) AND `VARIANT_WIDTHS` (`get-optimized-urls.ts:13`). Do NOT add xs to `ALL_VARIANTS` (:20) — that's the legacy pre-xs assumption list; adding it would falsely advertise a nonexistent variant. Cross-cutting: also affects project media (shared optimizer).
- Known drift NONE today: `proposals/DOCS.md#proposal-media` (:363-370,:386-387), `services/media/DOCS.md` stores table (:51), schema comment `db/schema/proposal-media-files.ts:13`, and MediaItem comments `components/media/types.ts:31,35` ALL accurately describe TODAY's private/presigned code. Design makes them stale in one move → DOCS+comment edits are BLOCKING same-PR, not follow-up.
- Migration precedents exist: `scripts/{backfill-media-bucket,migrate-r2-bucket,backfill-media-url-domain}.ts`. baseMediaColumns updatedAt = shared `updatedAt` helper w/ $onUpdate (`schema-helpers`) → backfill must NOT set updatedAt manually. Raw db.update in a one-off script is OK (scripts exempt from Rule-19 DAL-first; jobs/services are not).
- Ruling: derived `url` = JIT (domain+pathKey), NO url column → compliant with derived-values JIT-default (ADR-0005); keep the "no url column, JIT-derived" framing in DOCS rather than deleting the whole no-url decision.

## proposal-media optimization retry parity — Sub-plan 2.1 (last verified 2026-08-06)
- Context: audited PROPOSED design `docs/superpowers/specs/2026-08-06-proposal-media-optimization-parity-design.md` (add proposal retryOptimization + parameterized reset setter + backfill).
- SANCTIONED PRECEDENT (procedure calls DAL setter + fires optimize job, NO service): project `retryOptimization` (`trpc/routers/projects.router/media.router.ts:44-50`) = `agentProcedure` → `resetOptimizationStatus(id)` (DAL) + `void optimizeMediaJob.dispatch({ownerKind:'project',mediaId})`. Explicitly convention-blessed: service-architecture.md:332 lists "image optimization" as a VALID `void job.dispatch(...)` fire-and-forget from a tRPC mutation (the QStash-job rule at :260 carves cosmetic work out). No service is mandated between tRPC and DAL; proposal media router already does raw db writes itself (setVisibility `proposals.router/media.router.ts:68`). Spec's parity claim VERIFIED accurate.
- `resetOptimizationStatus` (old, `media-files/dal/server/queries.ts:50-55`) blast radius = EXACTLY 2 sites (import :8 + call :47 in projects media router), both rewritten by the spec → clean delete, matches ubiquitous-language:235 (radius fully mapped, every site rewritten/tallied) + delete-dead-code direction. VERIFIED grep.
- ⚠️ ADJACENT DEAD CODE (spec misses): the THREE sibling setters `setOptimizationProcessing/Complete/Failed` (`queries.ts:22-48`) have ZERO callers repo-wide (grep) — superseded by the table-parameterized versions in `optimization.ts` (which `optimizeMediaFile` actually uses, optimize-media.ts:5-8). Once reset moves out, the entire "Optimization status transitions" block in queries.ts is dead. Spec deletes only reset (:50-55), leaving 3 dead siblings → violates its OWN stated delete-dead-code principle. Recommend: delete all four (whole block) or tally.
- optimization.ts setters: `AnyMediaTable=any` contained-cast documented in file header (:8-14); proposed `resetMediaOptimizationStatus(table,id)` = `db.update(table).set({optimizationStatus:'pending'}).where(eq(table.id,id))` matches sibling shape exactly, no manual updatedAt. COMPLIANT.
- `optimizeMediaFile({ownerKind,mediaId})` (`services/media/optimize-media.ts:24`) idempotent (skips 'optimized' :34), self-sets processing/complete/failed, skips stream/no-pathKey/no-bucket. `optimizeMediaJob` payload `{ownerKind,mediaId}` (`providers/upstash/jobs/optimize-media.ts`). Backfill's inline `optimizeMediaFile` call VERIFIED correct.
- ⚠️ SCRIPT ENV DRIFT: spec cites `scripts/backfill-proposal-media-bucket.ts` as pattern + mandates `import './lib/load-env'`. But that precedent uses a HAND-ROLLED `drizzle(pg)` conn (DATABASE_URL vs DATABASE_DEV_URL by hand) — correct there because it does NOT import @/shared/db. Sub-plan 2.1's script calls `optimizeMediaFile` → transitively imports `@/shared/db` → server-env (self-loads dotenv AND resolves DRIZZLE_TARGET). Correct precedent = `backfill-wave2-children.ts`/`recompute-final-tcp.ts` (@/shared/db singleton, NO load-env; ledger scripts ruling 2026-07-24). Hazard: if the SELECT uses a 2nd hand-rolled conn while optimize uses @/shared/db, they can target DIFFERENT DBs when DRIZZLE_TARGET and the hand-picked URL disagree. Fix: SELECT via @/shared/db singleton (auto DRIZZLE_TARGET-guarded); load-env then redundant (harmless).
- Frontend: OptimizedImage (`shared/components/optimized-image.tsx:9-26`) has `onRetryOptimization?:(mediaFileId:number)=>void`; badges (Optimizing/Retry) render ONLY when prop present. Manager (`proposal-media-manager.tsx:88`) renders `<OptimizedImage file={row}…/>` WITHOUT the prop today; passing it = entire UI change. VERIFIED. use-proposal-media.ts `invalidate` (:10) → list queryKey; existing mutations use `{onSuccess:invalidate}` → spec's addition matches.
- SHOULD-CITE gap: `services/media/DOCS.md#optimize-dispatch-chain` (:84) documents create→dispatch but NOT a retry/reset recovery lever; spec Files list omits any DOCS update. Adding the owner-agnostic reset+retry contract is doc-worthy (parity note). Medium (project retry is also currently undocumented — no NEW drift introduced).

## Unwritten best practices (candidates for docs/)
- Isomorphic DAL tier: UL Single Unit (:21) documents `dal/` = "server/ and client/ subdirs" only — no home for shared client+server modules. De-facto workaround: client imports Zod schemas from `dal/server/lib/query/schemas.ts` (filter-parser-registry.ts:6). A neutral `src/shared/dal/lib/` matching root-lib semantics (UL:16 pure, no React) needs one doc sentence when created (query-toolkit.md layer diagram is the natural home). Noted 2026-07-24.
- nuqs import discipline: shared/isomorphic parser modules must import parsers from 'nuqs/server' (root 'nuqs' entry is client-only); no written rule yet. Noted 2026-07-24.
- Boolean-role-proxy → capability: `gatedPhoneSql(isSuperAdmin)` + `getAccessiblePipelines` omni-fork are role-proxy booleans with no written rule against them. Candidate permissions-topic rule: "gate on a CASL capability/subject, not on `role===x`/`isSuperAdmin` booleans." Promote via README decision tree.
- API-route URLs have no ROOTS builders and are exempt from the nav-path lint by design — urls-and-origins.md doesn't say this explicitly; candidate one-liner for that doc.
- `sanitizeFilename` collapses whitespace to `_` and does not strip `"` — callers embedding it in a quoted `Content-Disposition` header should strip quotes (header-injection edge). Candidate note wherever the helper lands.
- Money-as-integer-cents at the DAL boundary (dollars in domain/form shapes, `_cents` bigint columns, Math.round at the mapper) is enforced by precedent (proposal_incentives, final_tcp_cents) but has NO codebase-conventions doc — candidate `money.md` or a database-schema.md rule; W3 mints several new money columns, good promotion moment.
- environment.md "Key integrations" (:96,:101) predates the 2026-07-09 legacy-services scrub: still lists DocuSign as present-legacy and Notion as "temporary CRM (contacts)" though DocuSign is deleted and the CRM migration completed. Candidate doc touch-up (out of W3 scope).
